# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
.venv/
venv/
env/

# Build / packaging
build/
dist/
*.egg-info/
*.egg

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/
.tox/
.nox/

# Coverage
.coverage
.coverage.*
coverage.xml
htmlcov/

# Environment files
.env
.env.local
.env.*.local

# Frontend (client/)
node_modules/
client/dist/
client/coverage/
client/.tanstack/
# Env client : Vite charge nativement .env.development/.env.production — tout ignorer
# SAUF le gabarit documentaire. L'ordre est load-bearing : le glob AVANT la négation (D12).
client/.env
client/.env.*
!client/.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Claude Code : artefacts runtime locaux (verrou de tâches planifiées, contenu
# volatile pid/sessionId/timestamp) — le reste de .claude/ (commands, skills) est suivi.
.claude/scheduled_tasks.lock
.claude/*.lock
