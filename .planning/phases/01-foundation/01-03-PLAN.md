---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/Account.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountType.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountRepository.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountService.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountController.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountPermission.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/PermissionLevel.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/PermissionRepository.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/dto/CreateAccountRequest.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/dto/AccountDto.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/UserService.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/UserController.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/UpdateProfileRequest.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/UpdatePreferencesRequest.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/ChangePasswordRequest.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/CreateUserRequest.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/UserDto.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/category/Category.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/category/CategoryRepository.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/category/CategoryService.java
  - prosperity-api/src/main/java/fr/kalifazzia/prosperity/category/CategoryController.java
  - prosperity-api/src/test/java/fr/kalifazzia/prosperity/account/AccountControllerTest.java
  - prosperity-api/src/test/java/fr/kalifazzia/prosperity/user/UserServiceTest.java
  - prosperity-web/src/routes/(app)/+layout.svelte
  - prosperity-web/src/routes/(app)/+layout.server.ts
  - prosperity-web/src/routes/(app)/+page.svelte
  - prosperity-web/src/routes/(app)/accounts/+page.svelte
  - prosperity-web/src/routes/(app)/accounts/+page.server.ts
  - prosperity-web/src/routes/(app)/accounts/new/+page.svelte
  - prosperity-web/src/routes/(app)/accounts/new/+page.server.ts
  - prosperity-web/src/routes/(app)/settings/+layout.svelte
  - prosperity-web/src/routes/(app)/settings/+page.svelte
  - prosperity-web/src/routes/(app)/settings/profile/+page.svelte
  - prosperity-web/src/routes/(app)/settings/profile/+page.server.ts
  - prosperity-web/src/routes/(app)/settings/preferences/+page.svelte
  - prosperity-web/src/routes/(app)/settings/preferences/+page.server.ts
  - prosperity-web/src/routes/(app)/settings/security/+page.svelte
  - prosperity-web/src/routes/(app)/settings/security/+page.server.ts
  - prosperity-web/src/routes/(app)/settings/users/+page.svelte
  - prosperity-web/src/routes/(app)/settings/users/+page.server.ts
  - prosperity-web/src/lib/components/ui/Button.svelte
  - prosperity-web/src/lib/components/ui/Input.svelte
  - prosperity-web/src/lib/components/ui/Card.svelte
  - prosperity-web/src/lib/components/ui/Badge.svelte
  - prosperity-web/src/lib/components/ui/Select.svelte
  - prosperity-web/src/lib/components/ui/ColorPicker.svelte
  - prosperity-web/src/lib/components/AccountCard.svelte
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05
  - ACCT-01
  - ACCT-02
  - ACCT-03

must_haves:
  truths:
    - "User can create a Personal bank account with name, bank, currency, initial balance, and color"
    - "User can create a Shared bank account visible to both users"
    - "Personal accounts are only visible to their owner"
    - "User can update their display name from Settings > Profile"
    - "User can set theme (light/dark/system), default currency, language (FR/EN), and favorite categories"
    - "User can change their password from Settings > Security"
    - "Admin can create a Standard user from Settings > Users"
    - "Accounts display as colored cards grouped by 'Mes comptes' / 'Comptes partages'"
    - "Each account card shows dual balance (real + projected), even if both equal in Phase 1"
  artifacts:
    - path: "prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountService.java"
      provides: "Account CRUD with permission auto-granting"
      exports: ["createAccount", "getAccountsForUser"]
    - path: "prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/UserService.java"
      provides: "Profile update, preferences, password change, user creation"
      exports: ["updateProfile", "updatePreferences", "changePassword", "createUser"]
    - path: "prosperity-web/src/routes/(app)/accounts/+page.svelte"
      provides: "Account list with colored cards"
      min_lines: 40
    - path: "prosperity-web/src/routes/(app)/settings/+layout.svelte"
      provides: "Settings layout with sidebar navigation"
      min_lines: 20
    - path: "prosperity-web/src/lib/components/AccountCard.svelte"
      provides: "Colored account card with dual balance display"
      min_lines: 20
  key_links:
    - from: "prosperity-web/src/routes/(app)/accounts/+page.server.ts"
      to: "/api/accounts"
      via: "load function GET"
      pattern: "api/accounts"
    - from: "prosperity-api/src/main/java/.../AccountService.java"
      to: "AccountPermission"
      via: "auto-grant on create"
      pattern: "grantPermission\\|MANAGE\\|WRITE"
    - from: "prosperity-web/src/routes/(app)/settings/preferences/+page.server.ts"
      to: "/api/users/me/preferences"
      via: "form action PATCH"
      pattern: "api/users/me/preferences"
---

<objective>
Implement account management (CRUD with Personal/Shared permissions), user profile and preferences, Settings pages with sidebar, and account card UI with colored cards and dual balance display.

Purpose: Complete the Phase 1 feature set -- users can manage bank accounts with proper visibility, customize their profile and preferences, and admins can create the Standard user.
Output: Working accounts page with cards, full Settings (Profile, Preferences, Security, Users), and category management.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@docs/architecture.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement account and user backend -- CRUD, permissions, preferences, categories</name>
  <files>
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/Account.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountType.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountRepository.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountService.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountController.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/AccountPermission.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/PermissionLevel.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/PermissionRepository.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/dto/CreateAccountRequest.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/account/dto/AccountDto.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/UserService.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/UserController.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/UpdateProfileRequest.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/UpdatePreferencesRequest.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/ChangePasswordRequest.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/CreateUserRequest.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/UserDto.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/category/Category.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/category/CategoryRepository.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/category/CategoryService.java,
    prosperity-api/src/main/java/fr/kalifazzia/prosperity/category/CategoryController.java,
    prosperity-api/src/test/java/fr/kalifazzia/prosperity/account/AccountControllerTest.java,
    prosperity-api/src/test/java/fr/kalifazzia/prosperity/user/UserServiceTest.java
  </files>
  <behavior>
    - POST /api/accounts with type=PERSONAL creates account with MANAGE permission for owner only
    - POST /api/accounts with type=SHARED creates account with MANAGE for owner + WRITE for other user
    - GET /api/accounts returns only accounts where current user has permissions (personal = owner only, shared = both)
    - User A cannot see User B's personal accounts in GET /api/accounts
    - Both users can see shared accounts
    - PATCH /api/users/me/profile updates display name
    - PATCH /api/users/me/preferences updates preferences JSONB (theme, locale, currency, favoriteCategories)
    - POST /api/users/me/password changes password (requires old password match)
    - POST /api/users (admin only) creates Standard user with forcePasswordChange=true
    - Standard user cannot call POST /api/users (403)
    - GET /api/categories returns default + user-created categories
    - POST /api/categories creates custom category
    - Password change with wrong old password returns 400
  </behavior>
  <action>
    1. `AccountType.java`: Enum PERSONAL, SHARED.
    2. `PermissionLevel.java`: Enum MANAGE (owner), WRITE (can edit), READ (view only).

    3. `Account.java`: JPA entity mapped to `accounts`. Fields: id (UUID), name, bankName, accountType (AccountType enum STRING), ownerId (UUID FK), currency (String, default "EUR"), initialBalance (BigDecimal), currentBalance (BigDecimal), color (String), createdAt, updatedAt, version. Constructor accepting all required fields.

    4. `AccountPermission.java`: JPA entity mapped to `account_permissions`. Fields: id (UUID), accountId (UUID), userId (UUID), permissionLevel (PermissionLevel enum STRING). Unique constraint on (accountId, userId).

    5. `AccountRepository.java`: JpaRepository. Custom query: `@Query("SELECT a FROM Account a JOIN AccountPermission p ON a.id = p.accountId WHERE p.userId = :userId") List<Account> findAllByUserId(UUID userId)`.

    6. `PermissionRepository.java`: JpaRepository. `findByAccountIdAndUserId`, `findAllByAccountId`.

    7. `AccountService.java`:
       - `createAccount(CreateAccountRequest req, UUID currentUserId)`: Create Account entity with UUIDv7 id. Save. Grant MANAGE permission to currentUserId. If SHARED: find the other user via UserRepository, grant WRITE permission. Return AccountDto.
       - `getAccountsForUser(UUID userId)`: Query accounts where user has any permission. Return List<AccountDto> with accountType included for frontend grouping.
       - AccountDto includes: id, name, bankName, accountType, currency, initialBalance, currentBalance (both same in Phase 1), color, permissionLevel.

    8. `AccountController.java`:
       - POST `/api/accounts`: @RequestBody CreateAccountRequest, extract current user from SecurityContext, call service, return 201.
       - GET `/api/accounts`: extract current user, call service, return 200 with list.
       - `CreateAccountRequest` record: name (@NotBlank, @Size max 255), bankName, accountType (@NotNull), currency (default "EUR"), initialBalance (default 0), color.

    9. `UserService.java`:
       - `updateProfile(UUID userId, UpdateProfileRequest req)`: Update displayName. Return UserDto.
       - `updatePreferences(UUID userId, UpdatePreferencesRequest req)`: Merge preferences JSON. Return UserDto. Store as JSONB string: `{"theme":"dark","locale":"en","defaultCurrency":"EUR","favoriteCategories":["cat-id-1"]}`.
       - `changePassword(UUID userId, ChangePasswordRequest req)`: Verify old password with BCrypt, hash new password, save. Throw if old password wrong.
       - `createUser(CreateUserRequest req)`: @PreAuthorize("hasRole('ADMIN')"). Create user with STANDARD role, forcePasswordChange=true. Hash temp password. Return UserDto.
       - `getCurrentUser(UUID userId)`: Return UserDto with preferences.
       - `listUsers()`: @PreAuthorize("hasRole('ADMIN')"). Return all users as List<UserDto>.

    10. `UserController.java`:
        - GET `/api/users/me`: Return current user profile + preferences.
        - PATCH `/api/users/me/profile`: UpdateProfileRequest (displayName @NotBlank).
        - PATCH `/api/users/me/preferences`: UpdatePreferencesRequest (theme, locale, defaultCurrency, favoriteCategories).
        - POST `/api/users/me/password`: ChangePasswordRequest (oldPassword, newPassword, confirmPassword -- validate match).
        - GET `/api/users`: Admin only. List all users.
        - POST `/api/users`: Admin only. CreateUserRequest (email @Email, displayName, password).

    11. `Category.java`: Entity mapped to `categories`. Fields: id, nameKey, icon, isDefault, createdBy (nullable UUID), createdAt, updatedAt.
    12. `CategoryRepository.java`: `findByIsDefaultTrue()`, `findByCreatedBy(UUID userId)`.
    13. `CategoryService.java`: `getCategories(UUID userId)` returns defaults + user-created. `createCategory(nameKey, icon, userId)` creates custom category.
    14. `CategoryController.java`: GET `/api/categories`, POST `/api/categories`.

    15. Tests with @SpringBootTest + Testcontainers:
        - `AccountControllerTest`: Test create personal (only owner sees), create shared (both see), visibility isolation between users.
        - `UserServiceTest`: Test updateProfile, updatePreferences, changePassword (success + wrong old password), createUser (admin only), password stored as bcrypt.
  </action>
  <verify>
    <automated>cd prosperity-api && mvn test -Dtest="AccountControllerTest,UserServiceTest" -q 2>&1 | tail -10</automated>
  </verify>
  <done>Account CRUD respects Personal/Shared visibility. Profile, preferences, and password update work. Admin can create Standard user. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement accounts UI (colored cards, dual balance) and Settings pages (profile, preferences, security, users)</name>
  <files>
    prosperity-web/src/routes/(app)/+layout.svelte,
    prosperity-web/src/routes/(app)/+layout.server.ts,
    prosperity-web/src/routes/(app)/+page.svelte,
    prosperity-web/src/routes/(app)/accounts/+page.svelte,
    prosperity-web/src/routes/(app)/accounts/+page.server.ts,
    prosperity-web/src/routes/(app)/accounts/new/+page.svelte,
    prosperity-web/src/routes/(app)/accounts/new/+page.server.ts,
    prosperity-web/src/routes/(app)/settings/+layout.svelte,
    prosperity-web/src/routes/(app)/settings/+page.svelte,
    prosperity-web/src/routes/(app)/settings/profile/+page.svelte,
    prosperity-web/src/routes/(app)/settings/profile/+page.server.ts,
    prosperity-web/src/routes/(app)/settings/preferences/+page.svelte,
    prosperity-web/src/routes/(app)/settings/preferences/+page.server.ts,
    prosperity-web/src/routes/(app)/settings/security/+page.svelte,
    prosperity-web/src/routes/(app)/settings/security/+page.server.ts,
    prosperity-web/src/routes/(app)/settings/users/+page.svelte,
    prosperity-web/src/routes/(app)/settings/users/+page.server.ts,
    prosperity-web/src/lib/components/ui/Button.svelte,
    prosperity-web/src/lib/components/ui/Input.svelte,
    prosperity-web/src/lib/components/ui/Card.svelte,
    prosperity-web/src/lib/components/ui/Badge.svelte,
    prosperity-web/src/lib/components/ui/Select.svelte,
    prosperity-web/src/lib/components/ui/ColorPicker.svelte,
    prosperity-web/src/lib/components/AccountCard.svelte
  </files>
  <action>
    1. **UI Component Library** (src/lib/components/ui/) -- Create reusable components with Tailwind, Linear/Vercel design language:
       - `Button.svelte`: Props: variant (primary/secondary/ghost/danger), size (sm/md/lg), disabled, loading. Tailwind focus-visible ring, transitions.
       - `Input.svelte`: Props: label, name, type, error, placeholder, required. Floating or stacked label. Error styling with red border + message.
       - `Card.svelte`: Props: padding (default p-6), class (pass-through). Subtle border, rounded-lg, optional hover state.
       - `Badge.svelte`: Props: variant (default/success/warning/info), size. Rounded-full, font-medium.
       - `Select.svelte`: Props: label, name, options, value, error. Native select with custom styling.
       - `ColorPicker.svelte`: Grid of preset colors (8-10 colors). Clickable circles with checkmark on selected. Hidden input for form submission.

    2. **App layout** `(app)/+layout.svelte`:
       - Sidebar navigation: Prosperity logo/name at top, nav links (Dashboard, Accounts, Settings), user avatar/name at bottom with logout action.
       - Main content area with proper max-width and padding.
       - Responsive: sidebar on desktop, hamburger menu on mobile.
       - Dark mode support via Tailwind dark: classes.
       - Logout action: POST to a logout form action that clears cookies and redirects to /login.

    3. `(app)/+layout.server.ts`:
       - `load`: Fetch current user from `${API_URL}/api/users/me`. Return user data to all child routes. If fetch fails (token invalid), redirect to /login.

    4. `(app)/+page.svelte`: Dashboard placeholder -- simple welcome message with user's name. "Bienvenue, {displayName}" with i18n. Link to accounts. This page will be expanded in Phase 5.

    5. **Accounts page** `(app)/accounts/+page.svelte`:
       - Load accounts from +page.server.ts (GET /api/accounts).
       - Group accounts: "Mes comptes" (PERSONAL) / "Comptes partages" (SHARED) sections with i18n headings.
       - Render AccountCard components in a responsive grid (1 col mobile, 2 cols md, 3 cols lg).
       - "Nouveau compte" / "New account" button linking to /accounts/new.
       - Empty state with illustration/icon and CTA to create first account.

    6. `AccountCard.svelte`: Colored card displaying account info:
       - Left color bar or top border using the account's hex color.
       - Account name (bold), bank name (subtle).
       - Badge for PERSONAL / SHARED type.
       - Dual balance display: "Solde reel" / "Real balance" and "Solde projete" / "Projected balance" (both same value in Phase 1). Format with currency symbol and 2 decimal places.
       - Hover state with subtle elevation.

    7. **New account form** `(app)/accounts/new/+page.svelte` + `+page.server.ts`:
       - Form with: name (Input), bank name (Input), type (Select: Personnel/Partage), currency (Select: EUR default, common currencies), initial balance (Input type number, step 0.01), color (ColorPicker).
       - Form action: POST to `${API_URL}/api/accounts`. On success redirect to /accounts. On failure show errors.
       - Use:enhance for progressive enhancement.
       - All labels in i18n.

    8. **Settings layout** `(app)/settings/+layout.svelte`:
       - Left sidebar with sections: Profil, Preferences, Securite, Utilisateurs (admin only -- check user.role from parent layout data).
       - Active link highlighted.
       - On mobile: sidebar becomes a horizontal tab bar or dropdown menu (Claude's discretion -- horizontal tabs recommended for simplicity).
       - `(app)/settings/+page.svelte`: Redirect to /settings/profile (default section).

    9. **Settings > Profile** `(app)/settings/profile/`:
       - Show current email (read-only, grayed out).
       - Editable display name field.
       - Save button. Form action: PATCH to `${API_URL}/api/users/me/profile`.
       - Success toast/message.

    10. **Settings > Preferences** `(app)/settings/preferences/`:
        - Theme selector: 3-option toggle (Light / Dark / System). On change, immediately apply class and save.
        - Default currency: Select dropdown.
        - Language: Select (Francais / English). On change, update locale and reload page.
        - Favorite categories: Multi-select or checkbox list of available categories. Fetch from `${API_URL}/api/categories`.
        - Form action: PATCH to `${API_URL}/api/users/me/preferences`.

    11. **Settings > Security** `(app)/settings/security/`:
        - Password change form: old password, new password, confirm new password.
        - Client-side validation: new password min 8 chars, confirm matches.
        - Form action: POST to `${API_URL}/api/users/me/password`.
        - Success/error messages.

    12. **Settings > Users** (admin only) `(app)/settings/users/`:
        - List existing users (GET /api/users): show email, display name, role badge.
        - "Ajouter un utilisateur" / "Add user" form (inline or modal): email, display name, temporary password.
        - Form action: POST to `${API_URL}/api/users`.
        - Note displayed: "Le mot de passe devra etre change a la premiere connexion" / "Password must be changed on first login".
        - This page is only accessible to ADMIN role. If Standard user navigates here, redirect to /settings/profile.

    13. Add all new UI strings to `messages/fr.json` and `messages/en.json` for complete i18n coverage of accounts and settings pages.
  </action>
  <verify>
    <automated>cd prosperity-web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Accounts page shows colored cards grouped Personal/Shared with dual balance. Account creation form works. Settings has sidebar with Profile (edit name), Preferences (theme/language/currency/favorites), Security (change password), Users (admin creates Standard user). All pages use i18n strings. Responsive layout works on mobile.</done>
</task>

</tasks>

<verification>
1. `cd prosperity-api && mvn test -q` -- All backend tests pass (auth + accounts + users)
2. `cd prosperity-web && npm run build` -- Frontend builds without errors
3. Account visibility: personal accounts only visible to owner, shared visible to both
4. Settings sidebar shows 4 sections, Users only visible to admin
5. Account cards display color, name, bank, type badge, and dual balance
6. Theme toggle immediately applies dark/light mode
7. All UI text available in FR and EN
</verification>

<success_criteria>
- Create Personal account: only creator sees it in /accounts
- Create Shared account: both users see it in /accounts
- Account cards are visually rich: color bar, bank name, type badge, dual balance
- Profile update saves display name
- Preferences save theme, currency, language, favorite categories
- Password change requires old password, validates new password
- Admin can create Standard user with temporary password
- Settings sidebar navigation works on desktop and mobile
- All text is bilingual FR/EN via Paraglide
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
