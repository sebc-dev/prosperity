---
phase: 01-foundation
plan: 06
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - prosperity-web/src/routes/(app)/settings/+layout.svelte
  - prosperity-web/src/routes/(app)/settings/+page.svelte
  - prosperity-web/src/routes/(app)/settings/profile/+page.svelte
  - prosperity-web/src/routes/(app)/settings/profile/+page.server.ts
  - prosperity-web/src/routes/(app)/settings/preferences/+page.svelte
  - prosperity-web/src/routes/(app)/settings/preferences/+page.server.ts
  - prosperity-web/src/routes/(app)/settings/security/+page.svelte
  - prosperity-web/src/routes/(app)/settings/security/+page.server.ts
  - prosperity-web/src/routes/(app)/settings/users/+page.svelte
  - prosperity-web/src/routes/(app)/settings/users/+page.server.ts
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "User can update their display name from Settings > Profile"
    - "User can set theme (light/dark/system), default currency, language (FR/EN), and favorite categories"
    - "User can change their password from Settings > Security"
    - "Admin can create a Standard user from Settings > Users"
    - "Settings has sidebar navigation with Profile, Preferences, Security, Users sections"
  artifacts:
    - path: "prosperity-web/src/routes/(app)/settings/+layout.svelte"
      provides: "Settings layout with sidebar navigation"
      min_lines: 20
    - path: "prosperity-web/src/routes/(app)/settings/profile/+page.svelte"
      provides: "Profile editing page"
      min_lines: 20
    - path: "prosperity-web/src/routes/(app)/settings/preferences/+page.svelte"
      provides: "Preferences page with theme/locale/currency/favorites"
      min_lines: 30
    - path: "prosperity-web/src/routes/(app)/settings/users/+page.svelte"
      provides: "Admin user management page"
      min_lines: 30
  key_links:
    - from: "prosperity-web/src/routes/(app)/settings/profile/+page.server.ts"
      to: "/api/users/me/profile"
      via: "form action PATCH"
      pattern: "api/users/me/profile"
    - from: "prosperity-web/src/routes/(app)/settings/preferences/+page.server.ts"
      to: "/api/users/me/preferences"
      via: "form action PATCH"
      pattern: "api/users/me/preferences"
    - from: "prosperity-web/src/routes/(app)/settings/security/+page.server.ts"
      to: "/api/users/me/password"
      via: "form action POST"
      pattern: "api/users/me/password"
    - from: "prosperity-web/src/routes/(app)/settings/users/+page.server.ts"
      to: "/api/users"
      via: "form action POST"
      pattern: "api/users"
---

<objective>
Implement the Settings pages: Profile (edit display name), Preferences (theme, currency, language, favorite categories), Security (change password), and Users (admin creates Standard user).

Purpose: Complete user self-service management -- profile editing, personalization, password security, and admin user provisioning.
Output: Working Settings section with sidebar and all four sub-pages connected to backend API.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@.planning/phases/01-foundation/01-05-SUMMARY.md
@docs/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Settings layout and Profile/Preferences pages</name>
  <files>
    prosperity-web/src/routes/(app)/settings/+layout.svelte,
    prosperity-web/src/routes/(app)/settings/+page.svelte,
    prosperity-web/src/routes/(app)/settings/profile/+page.svelte,
    prosperity-web/src/routes/(app)/settings/profile/+page.server.ts,
    prosperity-web/src/routes/(app)/settings/preferences/+page.svelte,
    prosperity-web/src/routes/(app)/settings/preferences/+page.server.ts
  </files>
  <action>
    1. **Settings layout** `(app)/settings/+layout.svelte`:
       - Left sidebar with sections: Profil, Preferences, Securite, Utilisateurs (admin only -- check user.role from parent layout data).
       - Active link highlighted.
       - On mobile: sidebar becomes horizontal tabs (simpler than dropdown).
       - `(app)/settings/+page.svelte`: Redirect to /settings/profile (default section).

    2. **Settings > Profile** `(app)/settings/profile/`:
       - Show current email (read-only, grayed out).
       - Editable display name field.
       - Save button. Form action: PATCH to `${API_URL}/api/users/me/profile`.
       - Success toast/message.

    3. **Settings > Preferences** `(app)/settings/preferences/`:
       - Theme selector: 3-option toggle (Light / Dark / System). On change, immediately apply class and save.
       - Default currency: Select dropdown.
       - Language: Select (Francais / English). On change, update locale and reload page.
       - Favorite categories: Multi-select or checkbox list of available categories. Fetch from `${API_URL}/api/categories`.
       - Form action: PATCH to `${API_URL}/api/users/me/preferences`.

    4. Add all new UI strings to `messages/fr.json` and `messages/en.json` for settings pages.
  </action>
  <verify>
    <automated>cd prosperity-web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Settings layout with sidebar navigation. Profile page edits display name. Preferences page toggles theme, sets currency, language, favorite categories. All text bilingual.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Security and Users settings pages</name>
  <files>
    prosperity-web/src/routes/(app)/settings/security/+page.svelte,
    prosperity-web/src/routes/(app)/settings/security/+page.server.ts,
    prosperity-web/src/routes/(app)/settings/users/+page.svelte,
    prosperity-web/src/routes/(app)/settings/users/+page.server.ts
  </files>
  <action>
    1. **Settings > Security** `(app)/settings/security/`:
       - Password change form: old password, new password, confirm new password.
       - Client-side validation: new password min 8 chars, confirm matches.
       - Form action: POST to `${API_URL}/api/users/me/password`.
       - Success/error messages.

    2. **Settings > Users** (admin only) `(app)/settings/users/`:
       - List existing users (GET /api/users): show email, display name, role badge.
       - "Ajouter un utilisateur" / "Add user" form (inline or modal): email, display name, temporary password.
       - Form action: POST to `${API_URL}/api/users`.
       - Note displayed: "Le mot de passe devra etre change a la premiere connexion" / "Password must be changed on first login".
       - This page is only accessible to ADMIN role. If Standard user navigates here, redirect to /settings/profile.

    3. Add all new UI strings to `messages/fr.json` and `messages/en.json` for security and users pages.
  </action>
  <verify>
    <automated>cd prosperity-web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Security page changes password with old+new+confirm. Users page (admin only) lists users and creates Standard user with temporary password. Non-admin redirected away. All text bilingual.</done>
</task>

</tasks>

<verification>
1. `cd prosperity-web && npm run build` -- Frontend builds without errors
2. Settings sidebar shows 4 sections, Users only visible to admin
3. Theme toggle immediately applies dark/light mode
4. Password change validates old password and new password match
5. Admin can create Standard user with force password change note
6. All UI text available in FR and EN
</verification>

<success_criteria>
- Settings sidebar navigation works on desktop (sidebar) and mobile (tabs)
- Profile update saves display name via PATCH /api/users/me/profile
- Preferences save theme, currency, language, favorite categories via PATCH /api/users/me/preferences
- Password change requires old password, validates min 8 chars and match
- Admin can create Standard user with temporary password
- Users page only accessible to ADMIN role
- All text is bilingual FR/EN via Paraglide
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
