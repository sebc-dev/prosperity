---
phase: 01-foundation
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - prosperity-web/src/routes/(app)/settings/security/+page.server.ts
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - ACCT-01
  - ACCT-02
  - ACCT-03
  - INFR-01
  - INFR-02
  - INFR-03
  - INFR-04

must_haves:
  truths:
    - "User can change their password from Settings > Security without getting a 400 validation error"
  artifacts:
    - path: "prosperity-web/src/routes/(app)/settings/security/+page.server.ts"
      provides: "Password change form action with confirmPassword in POST body"
      contains: "confirmPassword"
  key_links:
    - from: "security/+page.server.ts"
      to: "/api/users/me/password"
      via: "apiClient POST"
      pattern: "confirmPassword"
---

<objective>
Fix password change wiring bug: add missing confirmPassword field to the POST body sent from the Settings > Security page to the backend.

Purpose: The backend ChangePasswordRequest DTO enforces @NotBlank on confirmPassword via @Valid. Without this field in the request body, every password change attempt returns a 400 validation error.
Output: Working password change flow from Settings > Security.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@prosperity-web/src/routes/(app)/settings/security/+page.server.ts
@prosperity-api/src/main/java/fr/kalifazzia/prosperity/user/dto/ChangePasswordRequest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add confirmPassword to password change POST body</name>
  <files>prosperity-web/src/routes/(app)/settings/security/+page.server.ts</files>
  <action>
In prosperity-web/src/routes/(app)/settings/security/+page.server.ts, on lines 26-28, the POST body currently sends only { oldPassword, newPassword }. Add confirmPassword to the POST body so it matches the backend ChangePasswordRequest DTO which requires all three fields.

Change line 26-28 from:
```typescript
const res = await api.post('/api/users/me/password', {
    oldPassword,
    newPassword
});
```

To:
```typescript
const res = await api.post('/api/users/me/password', {
    oldPassword,
    newPassword,
    confirmPassword
});
```

The confirmPassword variable is already extracted from the form data on line 10 and validated against newPassword on lines 20-22. It just needs to be included in the API call.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity && grep -n "confirmPassword" prosperity-web/src/routes/\(app\)/settings/security/+page.server.ts | grep "api.post\|post(" || grep -A5 "api.post" prosperity-web/src/routes/\(app\)/settings/security/+page.server.ts | grep "confirmPassword"</automated>
  </verify>
  <done>The POST body to /api/users/me/password includes confirmPassword alongside oldPassword and newPassword, matching the backend ChangePasswordRequest DTO contract.</done>
</task>

</tasks>

<verification>
- grep confirms confirmPassword appears in the api.post call body
- The file still compiles: `cd prosperity-web && npx svelte-check --threshold warning 2>&1 | tail -5`
</verification>

<success_criteria>
Password change POST body includes all three fields (oldPassword, newPassword, confirmPassword) required by the backend ChangePasswordRequest DTO, eliminating the 400 validation error.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md`
</output>
