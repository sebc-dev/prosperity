---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/.mvn/jvm.config
  - backend/.mvn/wrapper/maven-wrapper.properties
  - backend/mvnw
  - backend/mvnw.cmd
  - backend/checkstyle.xml
  - backend/src/main/resources/application.yml
  - backend/src/main/java/com/prosperity/ProsperityApplication.java
  - backend/src/main/java/com/prosperity/shared/Money.java
  - backend/src/main/java/com/prosperity/shared/MoneyConverter.java
  - backend/src/main/java/com/prosperity/shared/TransactionState.java
  - backend/src/main/java/com/prosperity/shared/AccountType.java
  - backend/src/main/java/com/prosperity/shared/RolloverPolicy.java
  - backend/src/main/java/com/prosperity/shared/EnvelopeScope.java
  - backend/src/main/java/com/prosperity/shared/TransactionSource.java
  - backend/src/main/java/com/prosperity/account/Account.java
  - backend/src/main/java/com/prosperity/account/AccountRepository.java
  - backend/src/main/java/com/prosperity/account/AccountAccess.java
  - backend/src/main/java/com/prosperity/account/AccountAccessRepository.java
  - backend/src/main/java/com/prosperity/account/AccessLevel.java
  - backend/src/main/java/com/prosperity/auth/User.java
  - backend/src/main/java/com/prosperity/auth/UserRepository.java
  - backend/src/main/java/com/prosperity/transaction/Transaction.java
  - backend/src/main/java/com/prosperity/transaction/TransactionRepository.java
  - backend/src/main/java/com/prosperity/category/Category.java
  - backend/src/main/java/com/prosperity/category/CategoryRepository.java
  - backend/src/main/java/com/prosperity/envelope/Envelope.java
  - backend/src/main/java/com/prosperity/envelope/EnvelopeRepository.java
  - backend/src/main/java/com/prosperity/envelope/EnvelopeAllocation.java
  - backend/src/main/java/com/prosperity/envelope/EnvelopeAllocationRepository.java
  - backend/src/main/java/com/prosperity/banking/BankConnector.java
  - backend/src/main/java/com/prosperity/banking/BankTransaction.java
  - backend/src/main/resources/db/migration/V001__create_users.sql
  - backend/src/main/resources/db/migration/V002__create_bank_accounts.sql
  - backend/src/main/resources/db/migration/V003__create_account_access.sql
  - backend/src/main/resources/db/migration/V004__create_categories.sql
  - backend/src/main/resources/db/migration/V005__create_transactions.sql
  - backend/src/main/resources/db/migration/V006__create_envelopes.sql
  - backend/src/test/java/com/prosperity/shared/MoneyTest.java
  - backend/src/test/java/com/prosperity/shared/TransactionStateTest.java
  - backend/src/test/java/com/prosperity/envelope/EnvelopeTest.java
  - backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java
  - backend/src/test/java/com/prosperity/ProsperityApplicationTest.java
autonomous: true
requirements:
  - INFR-04
  - INFR-05
  - INFR-06
  - INFR-07
  - INFR-08
  - INFR-09

must_haves:
  truths:
    - "./mvnw compile succeeds with Error Prone static analysis active"
    - "./mvnw test passes all domain model unit tests (Money, TransactionState, Envelope, ArchUnit)"
    - "./mvnw verify runs Checkstyle, Spotless, JaCoCo, and OWASP dependency-check without failure"
    - "Domain model enforces Money as BigDecimal with precision 2, rejects floating-point"
    - "TransactionState enum has exactly MANUAL_UNMATCHED, IMPORTED_UNMATCHED, MATCHED"
    - "ArchUnit validates no circular package dependencies and banking package uses interface abstraction"
  artifacts:
    - path: "backend/pom.xml"
      provides: "Maven project with Spring Boot 4.0.5 parent, all quality gate plugins"
      contains: "spring-boot-starter-parent"
    - path: "backend/src/main/java/com/prosperity/shared/Money.java"
      provides: "Money value object with BigDecimal, precision 2"
      contains: "record Money"
    - path: "backend/src/main/java/com/prosperity/banking/BankConnector.java"
      provides: "Abstract bank connector interface (D-03)"
      contains: "interface BankConnector"
    - path: "backend/src/main/resources/db/migration/V001__create_users.sql"
      provides: "First Flyway migration"
      contains: "CREATE TABLE users"
    - path: "backend/checkstyle.xml"
      provides: "Checkstyle configuration"
      contains: "Checker"
  key_links:
    - from: "backend/pom.xml"
      to: "Error Prone 2.48.0"
      via: "annotationProcessorPaths in maven-compiler-plugin"
      pattern: "error_prone_core"
    - from: "backend/pom.xml"
      to: "Spotless + google-java-format 1.35.0"
      via: "spotless-maven-plugin config"
      pattern: "spotless-maven-plugin"
    - from: "backend/src/main/java/com/prosperity/account/Account.java"
      to: "backend/src/main/java/com/prosperity/shared/Money.java"
      via: "@Convert(converter = MoneyConverter.class)"
      pattern: "MoneyConverter"
---

<objective>
Backend scaffolding: Spring Boot 4.0.x Maven project with complete domain model, JPA entities, Flyway migrations, unit tests, and all quality gate plugins configured.

Purpose: Establishes the entire backend foundation -- domain model, database schema, tests, and quality enforcement -- so that subsequent phases build on a solid, validated base.
Output: Compilable Spring Boot project with passing tests and quality gates.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@docs/agent_docs/architecture.md
@docs/agent_docs/database.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Maven project with Spring Boot 4.0.5, quality gate plugins, and configuration files</name>
  <files>
    backend/pom.xml,
    backend/.mvn/jvm.config,
    backend/.mvn/wrapper/maven-wrapper.properties,
    backend/mvnw,
    backend/mvnw.cmd,
    backend/checkstyle.xml,
    backend/src/main/resources/application.yml,
    backend/src/main/java/com/prosperity/ProsperityApplication.java
  </files>
  <read_first>
    docs/agent_docs/architecture.md,
    docs/agent_docs/database.md,
    .planning/phases/01-project-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Generate the Spring Boot Maven project using Spring Initializr CLI or by creating files manually. Do NOT use `spring init` -- create files directly.

    **1. Generate Maven Wrapper:**
    Run `mvn wrapper:wrapper -Dmaven=3.9.9` from within backend/ directory OR download mvnw files from https://github.com/apache/maven-wrapper. The wrapper must target Maven 3.9.x.

    Alternative: use `pnpm dlx @spring-boot/cli init` if available, or create manually.

    **2. Create `backend/pom.xml`** with:
    - Parent: `org.springframework.boot:spring-boot-starter-parent:4.0.5`
    - GroupId: `com.prosperity`, ArtifactId: `prosperity-backend`
    - Java version property: `<java.version>21</java.version>`
    - Dependencies:
      - `spring-boot-starter-web`
      - `spring-boot-starter-data-jpa`
      - `spring-boot-starter-validation`
      - `spring-boot-starter-actuator`
      - `org.flywaydb:flyway-core` (via Boot BOM)
      - `org.flywaydb:flyway-database-postgresql` (required for Flyway 11+ with PostgreSQL)
      - `org.postgresql:postgresql` (runtime scope)
      - Test: `spring-boot-starter-test`, `com.tngtech.archunit:archunit-junit5:1.3.0`
    - Plugins (in build/plugins):
      - **maven-compiler-plugin**: release=21, compilerArgs with `-XDcompilePolicy=simple`, `--should-stop=ifError=FLOW`, `-Xplugin:ErrorProne`, `-XDaddTypeAnnotationsToSymbol=true`, `-Xlint:all`. annotationProcessorPaths with `com.google.errorprone:error_prone_core:2.48.0` (per D-09, INFR-06, INFR-07)
      - **spotless-maven-plugin:2.43.0**: googleJavaFormat version=1.35.0, style=GOOGLE, bound to verify phase with `check` goal (per D-10, INFR-05)
      - **maven-checkstyle-plugin:3.6.0**: dependency override to `com.puppycrawl.tools:checkstyle:13.3.0`, configLocation=checkstyle.xml, failsOnError=true, consoleOutput=true, bound to verify with `check` goal (per D-10, INFR-04)
      - **jacoco-maven-plugin:0.8.15**: prepare-agent execution + report execution bound to verify phase. NO check goal, NO thresholds (per D-08, INFR-08)
      - **dependency-check-maven:12.2.0**: check goal bound to verify, failBuildOnCVSS=7 (per D-12, INFR-09)

    **3. Create `backend/.mvn/jvm.config`** with Error Prone JVM flags (required for Java 21):
    ```
    --add-exports jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED
    --add-exports jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED
    --add-exports jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED
    --add-exports jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED
    --add-exports jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED
    --add-exports jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED
    --add-exports jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED
    --add-exports jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED
    --add-opens jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED
    --add-opens jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED
    ```

    **4. Create `backend/checkstyle.xml`** based on Google's checks (google_checks.xml). Use `<module name="Checker">` as root. Include at minimum: TreeWalker with UnusedImports, RedundantImport, IllegalImport, PackageName, TypeName, MemberName, ParameterName, ConstantName, FinalClass, HideUtilityClassConstructor. Suppress "MissingSwitchDefault" (records in sealed hierarchies). Set tabWidth=2 to match google-java-format.

    **5. Create `backend/src/main/resources/application.yml`:**
    ```yaml
    spring:
      datasource:
        url: jdbc:postgresql://localhost:5432/prosperity
        username: prosperity
        password: prosperity
      jpa:
        hibernate:
          ddl-auto: validate
        open-in-view: false
      flyway:
        enabled: true
        locations: classpath:db/migration

    management:
      endpoints:
        web:
          exposure:
            include: health
      endpoint:
        health:
          show-details: always
    ```

    **6. Create `backend/src/main/java/com/prosperity/ProsperityApplication.java`:**
    ```java
    package com.prosperity;

    import org.springframework.boot.SpringApplication;
    import org.springframework.boot.autoconfigure.SpringBootApplication;

    @SpringBootApplication
    public class ProsperityApplication {
      public static void main(String[] args) {
        SpringApplication.run(ProsperityApplication.class, args);
      }
    }
    ```

    Format ALL Java files with google-java-format conventions: 2-space indent, Google import ordering.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw compile -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/pom.xml contains `spring-boot-starter-parent` with version `4.0.5`
    - backend/pom.xml contains `error_prone_core` with version `2.48.0`
    - backend/pom.xml contains `spotless-maven-plugin` with version `2.43.0`
    - backend/pom.xml contains `maven-checkstyle-plugin` with version `3.6.0`
    - backend/pom.xml contains `jacoco-maven-plugin` with version `0.8.15`
    - backend/pom.xml contains `dependency-check-maven` with version `12.2.0`
    - backend/pom.xml does NOT contain `<goal>check</goal>` inside jacoco-maven-plugin (report only, per D-08)
    - backend/.mvn/jvm.config contains `--add-exports jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED`
    - backend/checkstyle.xml contains `<module name="Checker">`
    - backend/checkstyle.xml contains `UnusedImports`
    - backend/src/main/resources/application.yml contains `ddl-auto: validate`
    - backend/src/main/resources/application.yml contains `flyway`
    - backend/src/main/java/com/prosperity/ProsperityApplication.java contains `@SpringBootApplication`
    - `./mvnw compile -q` exits with code 0
  </acceptance_criteria>
  <done>Maven project compiles successfully with Error Prone active, all quality gate plugins configured in pom.xml, application.yml configured for PostgreSQL + Flyway</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create domain model (entities, value objects, repositories, migrations) and unit tests</name>
  <files>
    backend/src/main/java/com/prosperity/shared/Money.java,
    backend/src/main/java/com/prosperity/shared/MoneyConverter.java,
    backend/src/main/java/com/prosperity/shared/TransactionState.java,
    backend/src/main/java/com/prosperity/shared/AccountType.java,
    backend/src/main/java/com/prosperity/shared/RolloverPolicy.java,
    backend/src/main/java/com/prosperity/shared/EnvelopeScope.java,
    backend/src/main/java/com/prosperity/shared/TransactionSource.java,
    backend/src/main/java/com/prosperity/account/Account.java,
    backend/src/main/java/com/prosperity/account/AccountRepository.java,
    backend/src/main/java/com/prosperity/account/AccountAccess.java,
    backend/src/main/java/com/prosperity/account/AccountAccessRepository.java,
    backend/src/main/java/com/prosperity/account/AccessLevel.java,
    backend/src/main/java/com/prosperity/auth/User.java,
    backend/src/main/java/com/prosperity/auth/UserRepository.java,
    backend/src/main/java/com/prosperity/transaction/Transaction.java,
    backend/src/main/java/com/prosperity/transaction/TransactionRepository.java,
    backend/src/main/java/com/prosperity/category/Category.java,
    backend/src/main/java/com/prosperity/category/CategoryRepository.java,
    backend/src/main/java/com/prosperity/envelope/Envelope.java,
    backend/src/main/java/com/prosperity/envelope/EnvelopeRepository.java,
    backend/src/main/java/com/prosperity/envelope/EnvelopeAllocation.java,
    backend/src/main/java/com/prosperity/envelope/EnvelopeAllocationRepository.java,
    backend/src/main/java/com/prosperity/banking/BankConnector.java,
    backend/src/main/java/com/prosperity/banking/BankTransaction.java,
    backend/src/main/resources/db/migration/V001__create_users.sql,
    backend/src/main/resources/db/migration/V002__create_bank_accounts.sql,
    backend/src/main/resources/db/migration/V003__create_account_access.sql,
    backend/src/main/resources/db/migration/V004__create_categories.sql,
    backend/src/main/resources/db/migration/V005__create_transactions.sql,
    backend/src/main/resources/db/migration/V006__create_envelopes.sql,
    backend/src/test/java/com/prosperity/shared/MoneyTest.java,
    backend/src/test/java/com/prosperity/shared/TransactionStateTest.java,
    backend/src/test/java/com/prosperity/envelope/EnvelopeTest.java,
    backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java,
    backend/src/test/java/com/prosperity/ProsperityApplicationTest.java
  </files>
  <read_first>
    backend/pom.xml,
    docs/agent_docs/database.md,
    docs/agent_docs/architecture.md,
    .planning/phases/01-project-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - MoneyTest: Money.of("10.50") creates Money with BigDecimal "10.50"
    - MoneyTest: Money.ofCents(1050) creates Money with BigDecimal "10.50"
    - MoneyTest: Money constructor rejects scale > 2 (throws IllegalArgumentException)
    - MoneyTest: Money has NO of(double) factory method (no floating-point)
    - MoneyTest: Money.add and Money.subtract work correctly
    - MoneyTest: Money.toCents returns correct long value
    - MoneyTest: Money constructor rejects null (throws NullPointerException)
    - TransactionStateTest: enum has exactly 3 values: MANUAL_UNMATCHED, IMPORTED_UNMATCHED, MATCHED
    - EnvelopeTest: Envelope with CARRY_OVER rollover preserves remaining budget
    - EnvelopeTest: Envelope with RESET rollover resets to zero
    - EnvelopeTest: Envelope overspend detection (consumed > budget)
    - ArchitectureTest: No circular package dependencies
    - ArchitectureTest: Classes in banking/ that are not interfaces must reside in a sub-package (banking.plaid/)
    - ArchitectureTest: Only shared/ package can be imported by all other packages
  </behavior>
  <action>
    Write tests FIRST (RED), then implement to make them pass (GREEN).

    **Value Objects (per D-05):**

    `Money.java` -- immutable record in `com.prosperity.shared`:
    ```java
    public record Money(BigDecimal amount) {
        public Money {
            Objects.requireNonNull(amount, "amount must not be null");
            if (amount.scale() > 2) {
                throw new IllegalArgumentException("Money precision cannot exceed 2 decimal places");
            }
            amount = amount.setScale(2, RoundingMode.HALF_UP);
        }
        public static Money of(String value) { return new Money(new BigDecimal(value)); }
        public static Money ofCents(long cents) { return new Money(BigDecimal.valueOf(cents, 2)); }
        // NO of(double) method -- floating-point forbidden
        public Money add(Money other) { return new Money(this.amount.add(other.amount)); }
        public Money subtract(Money other) { return new Money(this.amount.subtract(other.amount)); }
        public long toCents() { return amount.movePointRight(2).longValueExact(); }
    }
    ```

    `MoneyConverter.java` -- JPA AttributeConverter<Money, Long>:
    - `convertToDatabaseColumn(Money money)` returns `money.toCents()`
    - `convertToEntityAttribute(Long cents)` returns `Money.ofCents(cents)`
    - Annotate with `@Converter(autoApply = false)` (explicit `@Convert` on entities)

    `TransactionState.java` -- enum with exactly: `MANUAL_UNMATCHED`, `IMPORTED_UNMATCHED`, `MATCHED`

    **Enums:**
    - `AccountType.java`: `PERSONAL`, `SHARED`
    - `AccessLevel.java`: `READ`, `WRITE`, `ADMIN`
    - `RolloverPolicy.java`: `RESET`, `CARRY_OVER`
    - `EnvelopeScope.java`: `PERSONAL`, `SHARED`
    - `TransactionSource.java`: `MANUAL`, `PLAID`, `RECURRING`

    **JPA Entities (per D-04, D-06):**

    All entities use: `@Id @GeneratedValue(strategy = GenerationType.UUID) private UUID id;`, manual getters/setters (no Lombok), `@Column` annotations matching database.md schema, `TIMESTAMPTZ` mapped to `Instant` with `@Column(columnDefinition = "TIMESTAMPTZ")`.

    `User.java` (com.prosperity.auth): fields id, email (unique, not null, 255), passwordHash (not null, 255), displayName (not null, 100), role (not null, default "USER", 50), createdAt, updatedAt.

    `Account.java` (com.prosperity.account): fields id, name (not null, 100), accountType (enum AccountType, @Enumerated STRING), balance (@Convert MoneyConverter, column "balance_cents"), currency (not null, default "EUR", 3), plaidAccountId (nullable, 255), createdAt, updatedAt.

    `AccountAccess.java` (com.prosperity.account): fields id, user (@ManyToOne User, JoinColumn "user_id"), bankAccount (@ManyToOne Account, JoinColumn "bank_account_id"), accessLevel (enum AccessLevel, @Enumerated STRING). Unique constraint on (user_id, bank_account_id).

    `Category.java` (com.prosperity.category): fields id, name (not null, 100), parent (@ManyToOne self-referencing, nullable), plaidCategoryId (nullable, 255), createdAt.

    `Transaction.java` (com.prosperity.transaction): fields id, bankAccount (@ManyToOne Account), amount (@Convert MoneyConverter, column "amount_cents"), description (nullable, 500), category (nullable, 100), transactionDate (LocalDate, not null), source (enum TransactionSource, @Enumerated STRING), state (enum TransactionState, @Enumerated STRING, default MANUAL_UNMATCHED), plaidTransactionId (nullable, 255), pointed (boolean, default false), createdBy (@ManyToOne User, nullable), createdAt.

    `Envelope.java` (com.prosperity.envelope): fields id, bankAccount (@ManyToOne Account), name (not null, 100), scope (enum EnvelopeScope, @Enumerated STRING), owner (@ManyToOne User, nullable -- null if SHARED), budget (@Convert MoneyConverter, column "budget_cents"), rolloverPolicy (enum RolloverPolicy, @Enumerated STRING, default RESET), createdAt. Add business methods: `isOverspent(Money consumed)` returns true if consumed > budget; `rollover(Money remaining)` returns Money based on rolloverPolicy (RESET -> Money.of("0.00"), CARRY_OVER -> remaining).

    `EnvelopeAllocation.java` (com.prosperity.envelope): fields id, envelope (@ManyToOne Envelope), month (YearMonth mapped as String or Date), allocatedAmount (@Convert MoneyConverter), createdAt.

    **Banking abstraction (per D-03):**

    `BankConnector.java` (com.prosperity.banking): interface with methods:
    - `List<BankTransaction> fetchTransactions(String accessToken, LocalDate from, LocalDate to)`
    - `String createLinkToken(String userId)`

    `BankTransaction.java` (com.prosperity.banking): record with fields transactionId (String), amount (Money), description (String), date (LocalDate), categoryId (String), pending (boolean).

    **Repositories (per D-06):**
    - `UserRepository extends JpaRepository<User, UUID>`
    - `AccountRepository extends JpaRepository<Account, UUID>`
    - `AccountAccessRepository extends JpaRepository<AccountAccess, UUID>`
    - `TransactionRepository extends JpaRepository<Transaction, UUID>`
    - `CategoryRepository extends JpaRepository<Category, UUID>`
    - `EnvelopeRepository extends JpaRepository<Envelope, UUID>`
    - `EnvelopeAllocationRepository extends JpaRepository<EnvelopeAllocation, UUID>`

    **Flyway Migrations (per D-06):**

    `V001__create_users.sql`:
    ```sql
    CREATE TABLE users (
        id UUID PRIMARY KEY,
        email VARCHAR(255) NOT NULL UNIQUE,
        password_hash VARCHAR(255) NOT NULL,
        display_name VARCHAR(100) NOT NULL,
        role VARCHAR(50) NOT NULL DEFAULT 'USER',
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );
    ```

    `V002__create_bank_accounts.sql`:
    ```sql
    CREATE TABLE bank_accounts (
        id UUID PRIMARY KEY,
        name VARCHAR(100) NOT NULL,
        account_type VARCHAR(20) NOT NULL,
        balance_cents BIGINT NOT NULL DEFAULT 0,
        currency VARCHAR(3) NOT NULL DEFAULT 'EUR',
        plaid_account_id VARCHAR(255),
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
        updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );
    ```

    `V003__create_account_access.sql`:
    ```sql
    CREATE TABLE account_access (
        id UUID PRIMARY KEY,
        user_id UUID NOT NULL REFERENCES users(id),
        bank_account_id UUID NOT NULL REFERENCES bank_accounts(id),
        access_level VARCHAR(20) NOT NULL,
        UNIQUE(user_id, bank_account_id)
    );
    ```

    `V004__create_categories.sql`:
    ```sql
    CREATE TABLE categories (
        id UUID PRIMARY KEY,
        name VARCHAR(100) NOT NULL,
        parent_id UUID REFERENCES categories(id),
        plaid_category_id VARCHAR(255),
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );
    ```

    `V005__create_transactions.sql`:
    ```sql
    CREATE TABLE transactions (
        id UUID PRIMARY KEY,
        bank_account_id UUID NOT NULL REFERENCES bank_accounts(id),
        amount_cents BIGINT NOT NULL,
        description VARCHAR(500),
        category VARCHAR(100),
        transaction_date DATE NOT NULL,
        source VARCHAR(20) NOT NULL,
        state VARCHAR(30) NOT NULL DEFAULT 'MANUAL_UNMATCHED',
        plaid_transaction_id VARCHAR(255),
        pointed BOOLEAN NOT NULL DEFAULT FALSE,
        created_by UUID REFERENCES users(id),
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );
    ```

    `V006__create_envelopes.sql`:
    ```sql
    CREATE TABLE envelopes (
        id UUID PRIMARY KEY,
        bank_account_id UUID NOT NULL REFERENCES bank_accounts(id),
        name VARCHAR(100) NOT NULL,
        scope VARCHAR(20) NOT NULL,
        owner_id UUID REFERENCES users(id),
        budget_cents BIGINT NOT NULL DEFAULT 0,
        rollover_policy VARCHAR(20) NOT NULL DEFAULT 'RESET',
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );

    CREATE TABLE envelope_allocations (
        id UUID PRIMARY KEY,
        envelope_id UUID NOT NULL REFERENCES envelopes(id),
        month VARCHAR(7) NOT NULL,
        allocated_amount_cents BIGINT NOT NULL,
        created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    );
    ```

    **Tests (per D-07):**

    Write tests FIRST, then make them pass:
    - `MoneyTest.java`: test of(String), ofCents(long), add, subtract, toCents, null rejection, scale > 2 rejection, NO double factory
    - `TransactionStateTest.java`: verify exactly 3 values, verify valueOf works for each
    - `EnvelopeTest.java`: test isOverspent (true when consumed > budget, false otherwise), test rollover (RESET returns zero, CARRY_OVER returns remaining)
    - `ArchitectureTest.java`: ArchUnit rules -- no circular dependencies between feature packages, banking package top-level contains only interfaces, shared package importable by all
    - `ProsperityApplicationTest.java`: basic context loads test (will need @SpringBootTest with test DB or mock, can use `@SpringBootTest` with `spring.flyway.enabled=false` and `spring.datasource.url=jdbc:h2:mem:test` in test properties if H2 is added, OR simply test that the main class exists and has @SpringBootApplication annotation without loading context)

    Note: Full `@SpringBootTest` context load requires a running PostgreSQL. For Phase 1, make `ProsperityApplicationTest` a simple non-context test that verifies the application class exists and is annotated. Integration tests with Testcontainers come in later phases.

    Format ALL Java files with google-java-format style (2-space indent, Google import order). Run `./mvnw spotless:apply` after writing all files to auto-format.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw test -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/main/java/com/prosperity/shared/Money.java contains `record Money`
    - backend/src/main/java/com/prosperity/shared/Money.java does NOT contain `of(double`
    - backend/src/main/java/com/prosperity/shared/Money.java contains `ofCents(long cents)`
    - backend/src/main/java/com/prosperity/shared/TransactionState.java contains `MANUAL_UNMATCHED`
    - backend/src/main/java/com/prosperity/shared/TransactionState.java contains `IMPORTED_UNMATCHED`
    - backend/src/main/java/com/prosperity/shared/TransactionState.java contains `MATCHED`
    - backend/src/main/java/com/prosperity/banking/BankConnector.java contains `interface BankConnector`
    - backend/src/main/java/com/prosperity/account/Account.java contains `@Entity`
    - backend/src/main/java/com/prosperity/account/Account.java contains `MoneyConverter`
    - backend/src/main/java/com/prosperity/envelope/Envelope.java contains `isOverspent`
    - backend/src/main/java/com/prosperity/envelope/Envelope.java contains `rollover`
    - backend/src/main/resources/db/migration/V001__create_users.sql contains `CREATE TABLE users`
    - backend/src/main/resources/db/migration/V006__create_envelopes.sql contains `CREATE TABLE envelopes`
    - backend/src/main/resources/db/migration/V006__create_envelopes.sql contains `CREATE TABLE envelope_allocations`
    - backend/src/test/java/com/prosperity/shared/MoneyTest.java contains `@Test`
    - backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java contains `ArchUnit` or `ArchTest` or `ArchRuleDefinition`
    - `./mvnw test -q` exits with code 0
  </acceptance_criteria>
  <done>All domain model entities, value objects, repositories, and Flyway migrations created. Unit tests for Money, TransactionState, Envelope business rules, and ArchUnit architecture rules all pass. `./mvnw test` exits 0.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./mvnw compile` -- compiles with Error Prone active, no errors
2. `cd backend && ./mvnw test` -- all unit tests pass
3. `cd backend && ./mvnw spotless:check` -- format compliance
4. `cd backend && ./mvnw checkstyle:check` -- lint compliance
5. `cd backend && ./mvnw verify` -- full quality gate pass (compile + test + spotless + checkstyle + jacoco report + OWASP scan)
</verification>

<success_criteria>
- `./mvnw verify` passes with 0 errors (all quality gates green)
- MoneyTest, TransactionStateTest, EnvelopeTest, ArchitectureTest all pass
- 6 Flyway migration files exist in db/migration/
- All 5 entity classes (User, Account, Transaction, Category, Envelope) have @Entity annotation
- BankConnector.java is an interface (not a class)
- No `of(double` method exists in Money.java
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
