---
phase: 01-project-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - backend/src/main/java/com/prosperity/auth/User.java
  - backend/src/main/java/com/prosperity/auth/UserRepository.java
autonomous: true
requirements:
  - INFR-06

must_haves:
  truths:
    - "./mvnw compile succeeds with User entity and UserRepository"
    - "User entity uses UUID primary key with GenerationType.UUID"
    - "User entity has email (unique), passwordHash, displayName, role, createdAt, updatedAt"
  artifacts:
    - path: "backend/src/main/java/com/prosperity/auth/User.java"
      provides: "User JPA entity"
      contains: "@Entity"
    - path: "backend/src/main/java/com/prosperity/auth/UserRepository.java"
      provides: "Spring Data JPA repository for User"
      contains: "JpaRepository"
  key_links:
    - from: "backend/src/main/java/com/prosperity/auth/User.java"
      to: "backend/src/main/java/com/prosperity/ProsperityApplication.java"
      via: "@Entity scanned by Spring Boot"
      pattern: "@Entity"
---

<objective>
User entity and UserRepository: the auth domain's JPA entity that all other entities reference via foreign keys.

Purpose: Creates the User entity that Account, AccountAccess, Transaction, and Envelope depend on. Must exist before any entity with a User FK.
Output: 2 Java files (1 entity + 1 repository) that compile cleanly.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@docs/agent_docs/architecture.md
@docs/agent_docs/database.md

<interfaces>
<!-- From Plan 01 outputs -->
Backend Maven project at backend/ with:
- pom.xml with Spring Boot 4.0.5 parent, JPA, Flyway, PostgreSQL dependencies
- ./mvnw compile works

<!-- From Plan 02 outputs -->
Value objects and enums in backend/src/main/java/com/prosperity/:
- shared/Money.java: record Money(BigDecimal amount), of(String), ofCents(long), add, subtract, toCents
- shared/MoneyConverter.java: AttributeConverter<Money, Long>, @Converter(autoApply = false)
- shared/TransactionState.java: MANUAL_UNMATCHED, IMPORTED_UNMATCHED, MATCHED
- shared/AccountType.java: PERSONAL, SHARED
- shared/RolloverPolicy.java: RESET, CARRY_OVER
- shared/EnvelopeScope.java: PERSONAL, SHARED
- shared/TransactionSource.java: MANUAL, PLAID, RECURRING
- account/AccessLevel.java: READ, WRITE, ADMIN
- banking/BankConnector.java: interface
- banking/BankTransaction.java: record
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create User entity and UserRepository</name>
  <files>
    backend/src/main/java/com/prosperity/auth/User.java,
    backend/src/main/java/com/prosperity/auth/UserRepository.java
  </files>
  <read_first>
    backend/pom.xml,
    docs/agent_docs/database.md
  </read_first>
  <action>
    **User.java** (com.prosperity.auth, per D-04, D-06):

    `@Id @GeneratedValue(strategy = GenerationType.UUID) private UUID id;`, manual getters/setters (no Lombok), `@Column` annotations matching database.md schema, `TIMESTAMPTZ` mapped to `Instant` with `@Column(columnDefinition = "TIMESTAMPTZ")`.

    Fields: id (UUID), email (unique, not null, 255), passwordHash (not null, 255), displayName (not null, 100), role (not null, default "USER", 50), createdAt (Instant, TIMESTAMPTZ), updatedAt (Instant, TIMESTAMPTZ).

    Use `@Table(name = "users")`.

    **UserRepository** (com.prosperity.auth, per D-06):
    `UserRepository extends JpaRepository<User, UUID>`

    Format ALL Java files with google-java-format style. Run `./mvnw spotless:apply` after writing all files.
  </action>
  <acceptance_criteria>
    - User.java has @Entity and @Table(name = "users")
    - User.java has @Id @GeneratedValue(strategy = GenerationType.UUID)
    - email field has @Column(unique = true, nullable = false, length = 255)
    - createdAt and updatedAt use Instant with columnDefinition = "TIMESTAMPTZ"
    - UserRepository extends JpaRepository<User, UUID>
  </acceptance_criteria>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw compile -q 2>&1 | tail -5</automated>
  </verify>
  <done>User entity and UserRepository compile. User has UUID PK, email unique constraint, Instant timestamps, and all fields matching database.md schema.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./mvnw compile` -- User entity and repository compile with Error Prone active
2. `cd backend && ./mvnw spotless:check` -- format compliance
</verification>

<success_criteria>
- `./mvnw compile` passes with 0 errors
- User.java has @Entity annotation and UUID id with GenerationType.UUID
- UserRepository extends JpaRepository<User, UUID>
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-03-SUMMARY.md`
</output>
