---
phase: 01-project-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - backend/src/test/java/com/prosperity/shared/MoneyTest.java
  - backend/src/test/java/com/prosperity/shared/TransactionStateTest.java
  - backend/src/test/java/com/prosperity/envelope/EnvelopeTest.java
  - backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java
  - backend/src/test/java/com/prosperity/ProsperityApplicationTest.java
autonomous: true
requirements:
  - INFR-07

must_haves:
  truths:
    - "./mvnw test passes all domain model unit tests (Money, TransactionState, Envelope, ArchUnit)"
    - "MoneyTest validates BigDecimal precision, cent conversion, null rejection, no double factory"
    - "TransactionStateTest verifies exactly 3 enum values"
    - "EnvelopeTest verifies rollover and overspend business rules"
    - "ArchitectureTest validates no circular dependencies and banking abstraction"
  artifacts:
    - path: "backend/src/test/java/com/prosperity/shared/MoneyTest.java"
      provides: "Unit tests for Money value object"
      contains: "@Test"
    - path: "backend/src/test/java/com/prosperity/shared/TransactionStateTest.java"
      provides: "Unit tests for TransactionState enum"
      contains: "@Test"
    - path: "backend/src/test/java/com/prosperity/envelope/EnvelopeTest.java"
      provides: "Unit tests for Envelope business rules"
      contains: "isOverspent"
    - path: "backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java"
      provides: "ArchUnit architecture rules"
      contains: "ArchRuleDefinition"
  key_links:
    - from: "backend/src/test/java/com/prosperity/shared/MoneyTest.java"
      to: "backend/src/main/java/com/prosperity/shared/Money.java"
      via: "direct unit test"
      pattern: "Money\\.of"
    - from: "backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java"
      to: "com.prosperity.banking"
      via: "ArchUnit package rules"
      pattern: "banking"
---

<objective>
Unit tests: MoneyTest, TransactionStateTest, EnvelopeTest, ArchitectureTest, and ProsperityApplicationTest validating domain model correctness and architecture rules.

Purpose: Validates that the domain model created in Plan 02 enforces business rules correctly and architecture constraints (no circular deps, banking abstraction) are met.
Output: All tests pass with `./mvnw test`.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md

<interfaces>
<!-- From Plan 01+02 outputs -->
Backend Maven project at backend/ with:
- pom.xml with archunit-junit5:1.3.0 in test dependencies
- Money record: of(String), ofCents(long), add, subtract, toCents, no of(double)
- TransactionState enum: MANUAL_UNMATCHED, IMPORTED_UNMATCHED, MATCHED
- Envelope entity: isOverspent(Money), rollover(Money)
- BankConnector: interface in com.prosperity.banking
- All entities in their feature packages: auth/, account/, transaction/, category/, envelope/, banking/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit tests for domain model and architecture rules</name>
  <files>
    backend/src/test/java/com/prosperity/shared/MoneyTest.java,
    backend/src/test/java/com/prosperity/shared/TransactionStateTest.java,
    backend/src/test/java/com/prosperity/envelope/EnvelopeTest.java,
    backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java,
    backend/src/test/java/com/prosperity/ProsperityApplicationTest.java
  </files>
  <read_first>
    backend/pom.xml,
    backend/src/main/java/com/prosperity/shared/Money.java,
    backend/src/main/java/com/prosperity/shared/TransactionState.java,
    backend/src/main/java/com/prosperity/envelope/Envelope.java,
    backend/src/main/java/com/prosperity/banking/BankConnector.java
  </read_first>
  <behavior>
    - MoneyTest: Money.of("10.50") creates Money with BigDecimal "10.50"
    - MoneyTest: Money.ofCents(1050) creates Money with BigDecimal "10.50"
    - MoneyTest: Money constructor rejects scale > 2 (throws IllegalArgumentException)
    - MoneyTest: Money has NO of(double) factory method (no floating-point) -- verify via reflection
    - MoneyTest: Money.add and Money.subtract work correctly
    - MoneyTest: Money.toCents returns correct long value
    - MoneyTest: Money constructor rejects null (throws NullPointerException)
    - TransactionStateTest: enum has exactly 3 values: MANUAL_UNMATCHED, IMPORTED_UNMATCHED, MATCHED
    - EnvelopeTest: Envelope with CARRY_OVER rollover preserves remaining budget
    - EnvelopeTest: Envelope with RESET rollover resets to zero
    - EnvelopeTest: Envelope overspend detection (consumed > budget)
    - ArchitectureTest: No circular package dependencies
    - ArchitectureTest: Classes in banking/ that are not interfaces must reside in a sub-package (banking.plaid/)
    - ArchitectureTest: Only shared/ package can be imported by all other packages
  </behavior>
  <action>
    Write all test files. These test the domain model created in Plan 02.

    **MoneyTest.java** (per D-07): test of(String), ofCents(long), add, subtract, toCents, null rejection, scale > 2 rejection, verify NO `of(double)` method exists using reflection (`Money.class.getDeclaredMethod("of", double.class)` should throw NoSuchMethodException).

    **TransactionStateTest.java** (per D-07): verify `TransactionState.values().length == 3`, verify valueOf works for MANUAL_UNMATCHED, IMPORTED_UNMATCHED, MATCHED.

    **EnvelopeTest.java** (per D-07): Create Envelope instances and test:
    - `isOverspent(consumed)` returns true when consumed.amount > budget.amount, false otherwise
    - `rollover(remaining)` with RESET policy returns Money.of("0.00")
    - `rollover(remaining)` with CARRY_OVER policy returns the remaining amount

    **ArchitectureTest.java** (per D-02): ArchUnit rules:
    - `slices().matching("com.prosperity.(*)..").should().beFreeOfCycles()` -- no circular dependencies
    - Classes in `com.prosperity.banking` that are not in a sub-package should be interfaces -- ensures banking abstraction per D-03
    - `com.prosperity.shared` should be accessible from all packages (no restriction rule needed, just verify no outbound dependency from shared to feature packages)

    **ProsperityApplicationTest.java**: Simple non-context test that verifies the ProsperityApplication class exists and has @SpringBootApplication annotation. Do NOT use @SpringBootTest (requires running PostgreSQL). Use reflection: `ProsperityApplication.class.getAnnotation(SpringBootApplication.class) != null`.

    Format ALL test files with google-java-format style. Run `./mvnw spotless:apply` after writing all files.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw test -q 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 test files pass with `./mvnw test`. MoneyTest validates BigDecimal precision and no-double factory. TransactionStateTest validates 3 enum values. EnvelopeTest validates rollover and overspend. ArchitectureTest validates no cycles and banking abstraction. ProsperityApplicationTest validates annotation.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./mvnw test` -- all tests pass
2. `cd backend && ./mvnw verify` -- full quality gate pass (compile + test + spotless + checkstyle + jacoco report + OWASP scan)
</verification>

<success_criteria>
- `./mvnw test` passes with 0 failures
- `./mvnw verify` passes with 0 errors (all quality gates green)
- MoneyTest, TransactionStateTest, EnvelopeTest, ArchitectureTest, ProsperityApplicationTest all exist and pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-03-SUMMARY.md`
</output>
