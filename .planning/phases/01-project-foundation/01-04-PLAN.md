---
phase: 01-project-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - backend/src/main/java/com/prosperity/account/Account.java
  - backend/src/main/java/com/prosperity/account/AccountRepository.java
  - backend/src/main/java/com/prosperity/account/AccountAccess.java
  - backend/src/main/java/com/prosperity/account/AccountAccessRepository.java
autonomous: true
requirements:
  - INFR-06

must_haves:
  truths:
    - "./mvnw compile succeeds with Account, AccountAccess entities and repositories"
    - "Account uses @Convert(converter = MoneyConverter.class) for balance"
    - "AccountAccess has unique constraint on (user_id, bank_account_id)"
    - "AccountAccess references User via @ManyToOne"
  artifacts:
    - path: "backend/src/main/java/com/prosperity/account/Account.java"
      provides: "Account JPA entity with MoneyConverter"
      contains: "MoneyConverter"
    - path: "backend/src/main/java/com/prosperity/account/AccountAccess.java"
      provides: "AccountAccess JPA entity linking User to Account"
      contains: "AccessLevel"
    - path: "backend/src/main/java/com/prosperity/account/AccountRepository.java"
      provides: "Spring Data JPA repository for Account"
      contains: "JpaRepository"
    - path: "backend/src/main/java/com/prosperity/account/AccountAccessRepository.java"
      provides: "Spring Data JPA repository for AccountAccess"
      contains: "JpaRepository"
  key_links:
    - from: "backend/src/main/java/com/prosperity/account/Account.java"
      to: "backend/src/main/java/com/prosperity/shared/Money.java"
      via: "@Convert(converter = MoneyConverter.class)"
      pattern: "MoneyConverter"
    - from: "backend/src/main/java/com/prosperity/account/AccountAccess.java"
      to: "backend/src/main/java/com/prosperity/account/AccessLevel.java"
      via: "@Enumerated(EnumType.STRING)"
      pattern: "AccessLevel"
    - from: "backend/src/main/java/com/prosperity/account/AccountAccess.java"
      to: "backend/src/main/java/com/prosperity/auth/User.java"
      via: "@ManyToOne"
      pattern: "User"
---

<objective>
Account and AccountAccess entities with repositories: the account domain's JPA entities linking users to bank accounts with access levels.

Purpose: Creates Account (referenced by Transaction and Envelope) and AccountAccess (user-account permission mapping). Depends on User entity from Plan 03.
Output: 4 Java files (2 entities + 2 repositories) that compile cleanly.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@docs/agent_docs/architecture.md
@docs/agent_docs/database.md

<interfaces>
<!-- From Plan 01 outputs -->
Backend Maven project at backend/ with:
- pom.xml with Spring Boot 4.0.5 parent, JPA, Flyway, PostgreSQL dependencies
- ./mvnw compile works

<!-- From Plan 02 outputs -->
Value objects and enums:
- shared/Money.java: record Money(BigDecimal amount)
- shared/MoneyConverter.java: AttributeConverter<Money, Long>, @Converter(autoApply = false)
- shared/AccountType.java: PERSONAL, SHARED
- account/AccessLevel.java: READ, WRITE, ADMIN

<!-- From Plan 03 outputs -->
- auth/User.java: @Entity, UUID id, email, passwordHash, displayName, role, createdAt, updatedAt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Account, AccountAccess entities and repositories</name>
  <files>
    backend/src/main/java/com/prosperity/account/Account.java,
    backend/src/main/java/com/prosperity/account/AccountRepository.java,
    backend/src/main/java/com/prosperity/account/AccountAccess.java,
    backend/src/main/java/com/prosperity/account/AccountAccessRepository.java
  </files>
  <read_first>
    backend/pom.xml,
    backend/src/main/java/com/prosperity/shared/Money.java,
    backend/src/main/java/com/prosperity/shared/MoneyConverter.java,
    backend/src/main/java/com/prosperity/auth/User.java,
    docs/agent_docs/database.md
  </read_first>
  <action>
    All entities use: `@Id @GeneratedValue(strategy = GenerationType.UUID) private UUID id;`, manual getters/setters (no Lombok), `@Column` annotations matching database.md schema, `TIMESTAMPTZ` mapped to `Instant` with `@Column(columnDefinition = "TIMESTAMPTZ")`.

    **Account.java** (com.prosperity.account, per D-04, D-06):
    `@Table(name = "bank_accounts")`. Fields: id (UUID), name (not null, 100), accountType (enum AccountType, @Enumerated STRING), balance (@Convert MoneyConverter, column "balance_cents"), currency (not null, default "EUR", 3), plaidAccountId (nullable, 255), createdAt (Instant, TIMESTAMPTZ), updatedAt (Instant, TIMESTAMPTZ).

    **AccountAccess.java** (com.prosperity.account, per D-04, D-06):
    `@Table(name = "account_access", uniqueConstraints = @UniqueConstraint(columnNames = {"user_id", "bank_account_id"}))`. Fields: id (UUID), user (@ManyToOne User, JoinColumn "user_id"), bankAccount (@ManyToOne Account, JoinColumn "bank_account_id"), accessLevel (enum AccessLevel, @Enumerated STRING).

    **Repositories (per D-06):**
    - `AccountRepository extends JpaRepository<Account, UUID>`
    - `AccountAccessRepository extends JpaRepository<AccountAccess, UUID>`

    Format ALL Java files with google-java-format style. Run `./mvnw spotless:apply` after writing all files.
  </action>
  <acceptance_criteria>
    - Account.java has @Entity and @Table(name = "bank_accounts")
    - Account.java has @Convert(converter = MoneyConverter.class) on balance field with column name "balance_cents"
    - Account.java has accountType with @Enumerated(EnumType.STRING)
    - AccountAccess.java has @UniqueConstraint on (user_id, bank_account_id)
    - AccountAccess.java has @ManyToOne to User and Account
    - Both repositories extend JpaRepository with UUID
  </acceptance_criteria>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw compile -q 2>&1 | tail -5</automated>
  </verify>
  <done>Account and AccountAccess entities compile. Account uses MoneyConverter for balance, AccountAccess has unique constraint and ManyToOne to User. Both repositories extend JpaRepository.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./mvnw compile` -- Account and AccountAccess compile with Error Prone active
2. `cd backend && ./mvnw spotless:check` -- format compliance
</verification>

<success_criteria>
- `./mvnw compile` passes with 0 errors
- Account.java uses `@Convert(converter = MoneyConverter.class)` on balance
- AccountAccess.java has unique constraint on (user_id, bank_account_id)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-04-SUMMARY.md`
</output>
