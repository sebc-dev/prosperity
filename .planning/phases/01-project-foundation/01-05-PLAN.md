---
phase: 01-project-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - backend/src/main/java/com/prosperity/transaction/Transaction.java
  - backend/src/main/java/com/prosperity/transaction/TransactionRepository.java
autonomous: true
requirements:
  - INFR-06

must_haves:
  truths:
    - "./mvnw compile succeeds with Transaction entity and TransactionRepository"
    - "Transaction uses @Convert(converter = MoneyConverter.class) for amount"
    - "Transaction references Account via @ManyToOne and User via @ManyToOne (nullable)"
    - "Transaction uses TransactionState and TransactionSource enums with @Enumerated(STRING)"
  artifacts:
    - path: "backend/src/main/java/com/prosperity/transaction/Transaction.java"
      provides: "Transaction JPA entity with state and source enums"
      contains: "TransactionState"
    - path: "backend/src/main/java/com/prosperity/transaction/TransactionRepository.java"
      provides: "Spring Data JPA repository for Transaction"
      contains: "JpaRepository"
  key_links:
    - from: "backend/src/main/java/com/prosperity/transaction/Transaction.java"
      to: "backend/src/main/java/com/prosperity/shared/TransactionState.java"
      via: "@Enumerated(EnumType.STRING)"
      pattern: "TransactionState"
    - from: "backend/src/main/java/com/prosperity/transaction/Transaction.java"
      to: "backend/src/main/java/com/prosperity/account/Account.java"
      via: "@ManyToOne"
      pattern: "Account"
---

<objective>
Transaction entity and TransactionRepository: the transaction domain's JPA entity linking transactions to accounts and users.

Purpose: Creates the Transaction entity that records financial movements. Depends on Account (Plan 04) and User (Plan 03) for foreign key relationships.
Output: 2 Java files (1 entity + 1 repository) that compile cleanly.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@docs/agent_docs/architecture.md
@docs/agent_docs/database.md

<interfaces>
<!-- From Plan 02 outputs -->
Value objects and enums:
- shared/Money.java: record Money(BigDecimal amount)
- shared/MoneyConverter.java: AttributeConverter<Money, Long>
- shared/TransactionState.java: MANUAL_UNMATCHED, IMPORTED_UNMATCHED, MATCHED
- shared/TransactionSource.java: MANUAL, PLAID, RECURRING

<!-- From Plan 03 outputs -->
- auth/User.java: @Entity, UUID id

<!-- From Plan 04 outputs -->
- account/Account.java: @Entity, @Table(name = "bank_accounts"), UUID id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Transaction entity and TransactionRepository</name>
  <files>
    backend/src/main/java/com/prosperity/transaction/Transaction.java,
    backend/src/main/java/com/prosperity/transaction/TransactionRepository.java
  </files>
  <read_first>
    backend/pom.xml,
    backend/src/main/java/com/prosperity/shared/MoneyConverter.java,
    backend/src/main/java/com/prosperity/shared/TransactionState.java,
    backend/src/main/java/com/prosperity/shared/TransactionSource.java,
    backend/src/main/java/com/prosperity/account/Account.java,
    backend/src/main/java/com/prosperity/auth/User.java,
    docs/agent_docs/database.md
  </read_first>
  <action>
    **Transaction.java** (com.prosperity.transaction, per D-04, D-06):

    `@Id @GeneratedValue(strategy = GenerationType.UUID) private UUID id;`, manual getters/setters (no Lombok), `@Column` annotations matching database.md schema, `TIMESTAMPTZ` mapped to `Instant` with `@Column(columnDefinition = "TIMESTAMPTZ")`.

    `@Table(name = "transactions")`. Fields: id (UUID), bankAccount (@ManyToOne Account, JoinColumn "bank_account_id", not null), amount (@Convert MoneyConverter, column "amount_cents"), description (nullable, 500), category (nullable, 100), transactionDate (LocalDate, not null), source (enum TransactionSource, @Enumerated STRING), state (enum TransactionState, @Enumerated STRING, default MANUAL_UNMATCHED), plaidTransactionId (nullable, 255), pointed (boolean, default false), createdBy (@ManyToOne User, nullable, JoinColumn "created_by"), createdAt (Instant, TIMESTAMPTZ).

    **TransactionRepository** (com.prosperity.transaction, per D-06):
    `TransactionRepository extends JpaRepository<Transaction, UUID>`

    Format ALL Java files with google-java-format style. Run `./mvnw spotless:apply` after writing all files.
  </action>
  <acceptance_criteria>
    - Transaction.java has @Entity and @Table(name = "transactions")
    - Transaction.java has @Convert(converter = MoneyConverter.class) on amount field with column name "amount_cents"
    - Transaction.java has @ManyToOne Account (not null) and @ManyToOne User (nullable)
    - Transaction.java has @Enumerated(EnumType.STRING) on source and state fields
    - state field defaults to MANUAL_UNMATCHED
    - TransactionRepository extends JpaRepository<Transaction, UUID>
  </acceptance_criteria>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw compile -q 2>&1 | tail -5</automated>
  </verify>
  <done>Transaction entity and TransactionRepository compile. Transaction uses MoneyConverter for amount, references Account and User, uses TransactionState and TransactionSource enums.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./mvnw compile` -- Transaction entity and repository compile with Error Prone active
2. `cd backend && ./mvnw spotless:check` -- format compliance
</verification>

<success_criteria>
- `./mvnw compile` passes with 0 errors
- Transaction.java uses `@Convert(converter = MoneyConverter.class)` on amount
- Transaction.java has @ManyToOne to Account and User
- Transaction.java uses TransactionState and TransactionSource enums
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-05-SUMMARY.md`
</output>
