---
phase: 01-project-foundation
plan: 07
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - backend/src/main/java/com/prosperity/category/Category.java
  - backend/src/main/java/com/prosperity/category/CategoryRepository.java
  - backend/src/main/java/com/prosperity/envelope/Envelope.java
  - backend/src/main/java/com/prosperity/envelope/EnvelopeRepository.java
  - backend/src/main/java/com/prosperity/envelope/EnvelopeAllocation.java
  - backend/src/main/java/com/prosperity/envelope/EnvelopeAllocationRepository.java
autonomous: true
requirements:
  - INFR-06

must_haves:
  truths:
    - "./mvnw compile succeeds with Category, Envelope, EnvelopeAllocation entities and repositories"
    - "Envelope has isOverspent and rollover business methods"
    - "Envelope uses @Convert(converter = MoneyConverter.class) for budget"
    - "Category has self-referencing @ManyToOne for hierarchical categories"
    - "EnvelopeAllocation uses MoneyConverter for allocatedAmount"
  artifacts:
    - path: "backend/src/main/java/com/prosperity/category/Category.java"
      provides: "Category JPA entity with self-referencing parent"
      contains: "@Entity"
    - path: "backend/src/main/java/com/prosperity/envelope/Envelope.java"
      provides: "Envelope entity with business methods"
      contains: "isOverspent"
    - path: "backend/src/main/java/com/prosperity/envelope/EnvelopeAllocation.java"
      provides: "EnvelopeAllocation entity for monthly budget allocations"
      contains: "MoneyConverter"
    - path: "backend/src/main/java/com/prosperity/category/CategoryRepository.java"
      provides: "Spring Data JPA repository for Category"
      contains: "JpaRepository"
    - path: "backend/src/main/java/com/prosperity/envelope/EnvelopeRepository.java"
      provides: "Spring Data JPA repository for Envelope"
      contains: "JpaRepository"
    - path: "backend/src/main/java/com/prosperity/envelope/EnvelopeAllocationRepository.java"
      provides: "Spring Data JPA repository for EnvelopeAllocation"
      contains: "JpaRepository"
  key_links:
    - from: "backend/src/main/java/com/prosperity/envelope/Envelope.java"
      to: "backend/src/main/java/com/prosperity/shared/Money.java"
      via: "@Convert(converter = MoneyConverter.class)"
      pattern: "MoneyConverter"
    - from: "backend/src/main/java/com/prosperity/envelope/Envelope.java"
      to: "backend/src/main/java/com/prosperity/account/Account.java"
      via: "@ManyToOne"
      pattern: "Account"
    - from: "backend/src/main/java/com/prosperity/category/Category.java"
      to: "backend/src/main/java/com/prosperity/category/Category.java"
      via: "@ManyToOne self-referencing parent"
      pattern: "parent"
---

<objective>
Category, Envelope, and EnvelopeAllocation entities with repositories: the envelope/category domain layer with budget business logic.

Purpose: Creates Category (hierarchical, self-referencing), Envelope (with rollover and overspend business methods), and EnvelopeAllocation (monthly budget allocations). These are tightly coupled via budget allocation logic.
Output: 6 Java files (3 entities + 3 repositories) that compile cleanly.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@docs/agent_docs/architecture.md
@docs/agent_docs/database.md

<interfaces>
<!-- From Plan 02 outputs -->
Value objects and enums:
- shared/Money.java: record Money(BigDecimal amount), of(String), ofCents(long), add, subtract, toCents
- shared/MoneyConverter.java: AttributeConverter<Money, Long>, @Converter(autoApply = false)
- shared/RolloverPolicy.java: RESET, CARRY_OVER
- shared/EnvelopeScope.java: PERSONAL, SHARED

<!-- From Plan 03 outputs -->
- auth/User.java: @Entity, UUID id

<!-- From Plan 04 outputs -->
- account/Account.java: @Entity, @Table(name = "bank_accounts"), UUID id
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Category, Envelope, EnvelopeAllocation entities and repositories</name>
  <files>
    backend/src/main/java/com/prosperity/category/Category.java,
    backend/src/main/java/com/prosperity/category/CategoryRepository.java,
    backend/src/main/java/com/prosperity/envelope/Envelope.java,
    backend/src/main/java/com/prosperity/envelope/EnvelopeRepository.java,
    backend/src/main/java/com/prosperity/envelope/EnvelopeAllocation.java,
    backend/src/main/java/com/prosperity/envelope/EnvelopeAllocationRepository.java
  </files>
  <read_first>
    backend/pom.xml,
    backend/src/main/java/com/prosperity/shared/Money.java,
    backend/src/main/java/com/prosperity/shared/MoneyConverter.java,
    backend/src/main/java/com/prosperity/shared/RolloverPolicy.java,
    backend/src/main/java/com/prosperity/shared/EnvelopeScope.java,
    backend/src/main/java/com/prosperity/account/Account.java,
    backend/src/main/java/com/prosperity/auth/User.java,
    docs/agent_docs/database.md
  </read_first>
  <action>
    All entities use: `@Id @GeneratedValue(strategy = GenerationType.UUID) private UUID id;`, manual getters/setters (no Lombok), `@Column` annotations matching database.md schema, `TIMESTAMPTZ` mapped to `Instant` with `@Column(columnDefinition = "TIMESTAMPTZ")`.

    **Category.java** (com.prosperity.category, per D-04, D-06):
    `@Table(name = "categories")`. Fields: id (UUID), name (not null, 100), parent (@ManyToOne self-referencing Category, nullable, JoinColumn "parent_id"), plaidCategoryId (nullable, 255), createdAt (Instant, TIMESTAMPTZ).

    **Envelope.java** (com.prosperity.envelope, per D-04, D-06):
    `@Table(name = "envelopes")`. Fields: id (UUID), bankAccount (@ManyToOne Account, JoinColumn "bank_account_id", not null), name (not null, 100), scope (enum EnvelopeScope, @Enumerated STRING), owner (@ManyToOne User, nullable -- null if SHARED, JoinColumn "owner_id"), budget (@Convert MoneyConverter, column "budget_cents"), rolloverPolicy (enum RolloverPolicy, @Enumerated STRING, default RESET), createdAt (Instant, TIMESTAMPTZ).

    Add business methods:
    - `isOverspent(Money consumed)` returns true if consumed.amount() > budget.amount() (using compareTo)
    - `rollover(Money remaining)` returns Money based on rolloverPolicy: RESET -> Money.of("0.00"), CARRY_OVER -> remaining

    **EnvelopeAllocation.java** (com.prosperity.envelope, per D-04, D-06):
    `@Table(name = "envelope_allocations")`. Fields: id (UUID), envelope (@ManyToOne Envelope, JoinColumn "envelope_id", not null), month (YearMonth mapped as String with @Column(length = 7) -- store as "2026-03" format, use getter/setter to convert), allocatedAmount (@Convert MoneyConverter, column "allocated_amount_cents"), createdAt (Instant, TIMESTAMPTZ).

    **Repositories (per D-06):**
    - `CategoryRepository extends JpaRepository<Category, UUID>`
    - `EnvelopeRepository extends JpaRepository<Envelope, UUID>`
    - `EnvelopeAllocationRepository extends JpaRepository<EnvelopeAllocation, UUID>`

    Format ALL Java files with google-java-format style. Run `./mvnw spotless:apply` after writing all files.
  </action>
  <acceptance_criteria>
    - Category.java has @Entity and @Table(name = "categories") with self-referencing @ManyToOne parent
    - Envelope.java has @Entity and @Table(name = "envelopes")
    - Envelope.java has @Convert(converter = MoneyConverter.class) on budget field with column name "budget_cents"
    - Envelope.java contains isOverspent(Money) method returning boolean
    - Envelope.java contains rollover(Money) method returning Money
    - EnvelopeAllocation.java has @Convert(converter = MoneyConverter.class) on allocatedAmount
    - All 3 repositories extend JpaRepository with UUID
  </acceptance_criteria>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw compile -q 2>&1 | tail -5</automated>
  </verify>
  <done>Category, Envelope, and EnvelopeAllocation entities compile. Envelope has isOverspent and rollover business methods. Category has self-referencing parent. All use MoneyConverter where applicable. All 3 repositories extend JpaRepository.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./mvnw compile` -- all entities and repositories compile with Error Prone active
2. `cd backend && ./mvnw spotless:check` -- format compliance
3. Verify Envelope has isOverspent and rollover methods
</verification>

<success_criteria>
- `./mvnw compile` passes with 0 errors
- Envelope.java contains `isOverspent` and `rollover` business methods
- Category.java has self-referencing @ManyToOne parent
- EnvelopeAllocation.java uses MoneyConverter for allocatedAmount
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-07-SUMMARY.md`
</output>
