---
phase: 01-project-foundation
plan: 10
type: execute
wave: 6
depends_on:
  - 01-07
files_modified:
  - backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java
autonomous: true
requirements:
  - INFR-07

must_haves:
  truths:
    - "./mvnw test passes ArchitectureTest validating no circular dependencies and banking abstraction"
    - "ArchUnit rules enforce no circular package dependencies"
    - "ArchUnit rules enforce banking/ top-level classes are interfaces only"
  artifacts:
    - path: "backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java"
      provides: "ArchUnit architecture rules"
      contains: "ArchRuleDefinition"
  key_links:
    - from: "backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java"
      to: "com.prosperity.banking"
      via: "ArchUnit package rules"
      pattern: "banking"
---

<objective>
ArchUnit architecture test: validates codebase structure rules including no circular dependencies and banking abstraction enforcement.

Purpose: Ensures the layered architecture constraints (per D-02) are automatically enforced. Banking package must expose only interfaces at the top level (per D-03). All feature packages must be cycle-free.
Output: 1 test file that passes with `./mvnw test`.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md

<interfaces>
<!-- From Plans 01-07 outputs -->
Backend Maven project at backend/ with:
- pom.xml with archunit-junit5:1.3.0 in test dependencies
- BankConnector: interface in com.prosperity.banking
- BankTransaction: record in com.prosperity.banking
- All entities in their feature packages: auth/, account/, transaction/, category/, envelope/, banking/
- shared/ package contains value objects and enums
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArchUnit architecture test</name>
  <files>
    backend/src/test/java/com/prosperity/architecture/ArchitectureTest.java
  </files>
  <read_first>
    backend/pom.xml,
    backend/src/main/java/com/prosperity/banking/BankConnector.java
  </read_first>
  <action>
    **ArchitectureTest.java** (per D-02): ArchUnit rules:
    - `slices().matching("com.prosperity.(*)..").should().beFreeOfCycles()` -- no circular dependencies
    - Classes in `com.prosperity.banking` that are not in a sub-package should be interfaces or records -- ensures banking abstraction per D-03. Use ArchUnit rule: classes that reside in package `com.prosperity.banking` (not sub-packages) and are not records should be interfaces.
    - `com.prosperity.shared` should not depend on feature packages (shared has no outbound dependency to auth, account, transaction, category, envelope, banking) -- ensures shared stays foundational.

    Use `@AnalyzeClasses(packages = "com.prosperity")` annotation on the test class.
    Import from `com.tngtech.archunit.junit.AnalyzeClasses`, `com.tngtech.archunit.junit.ArchTest`, `com.tngtech.archunit.lang.ArchRule`.

    Format with google-java-format style. Run `./mvnw spotless:apply` after writing.
  </action>
  <acceptance_criteria>
    - ArchitectureTest.java uses @AnalyzeClasses(packages = "com.prosperity")
    - Contains rule for no circular package dependencies using slices()
    - Contains rule enforcing banking/ top-level classes are interfaces or records
    - Contains rule enforcing shared/ has no outbound deps to feature packages
    - Test passes with ./mvnw test
  </acceptance_criteria>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw test -pl . -Dtest="ArchitectureTest" -q 2>&1 | tail -10</automated>
  </verify>
  <done>ArchitectureTest validates no circular dependencies, banking abstraction (interfaces only at top level), and shared/ isolation. Test passes.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./mvnw test -Dtest="ArchitectureTest"` -- passes
2. `cd backend && ./mvnw spotless:check` -- format compliance
</verification>

<success_criteria>
- `./mvnw test` passes ArchitectureTest with 0 failures
- ArchitectureTest enforces no cycles, banking abstraction, and shared isolation
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-10-SUMMARY.md`
</output>
