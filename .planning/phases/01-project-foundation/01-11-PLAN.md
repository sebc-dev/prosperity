---
phase: 01-project-foundation
plan: 11
type: execute
wave: 6
depends_on:
  - 01-07
files_modified:
  - backend/src/test/java/com/prosperity/ProsperityApplicationTest.java
autonomous: true
requirements:
  - INFR-07

must_haves:
  truths:
    - "./mvnw test passes ProsperityApplicationTest validating @SpringBootApplication annotation"
    - "Test does NOT use @SpringBootTest (no running PostgreSQL required)"
  artifacts:
    - path: "backend/src/test/java/com/prosperity/ProsperityApplicationTest.java"
      provides: "Boot annotation test (no context loading)"
      contains: "SpringBootApplication"
  key_links:
    - from: "backend/src/test/java/com/prosperity/ProsperityApplicationTest.java"
      to: "backend/src/main/java/com/prosperity/ProsperityApplication.java"
      via: "reflection annotation check"
      pattern: "ProsperityApplication"
---

<objective>
ProsperityApplicationTest: validates that the Spring Boot application class exists and has the correct annotation, without loading the full Spring context.

Purpose: Ensures the application entry point is correctly annotated. Uses reflection instead of @SpringBootTest to avoid requiring a running PostgreSQL instance.
Output: 1 test file that passes with `./mvnw test`.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md

<interfaces>
<!-- From Plan 01 outputs -->
- ProsperityApplication.java: @SpringBootApplication, main method
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProsperityApplicationTest</name>
  <files>
    backend/src/test/java/com/prosperity/ProsperityApplicationTest.java
  </files>
  <read_first>
    backend/src/main/java/com/prosperity/ProsperityApplication.java
  </read_first>
  <action>
    **ProsperityApplicationTest.java**: Simple non-context test that verifies the ProsperityApplication class exists and has @SpringBootApplication annotation. Do NOT use @SpringBootTest (requires running PostgreSQL). Use reflection: `ProsperityApplication.class.getAnnotation(SpringBootApplication.class) != null`.

    ```java
    import static org.assertj.core.api.Assertions.assertThat;
    import org.junit.jupiter.api.Test;
    import org.springframework.boot.autoconfigure.SpringBootApplication;

    class ProsperityApplicationTest {
        @Test
        void applicationHasSpringBootAnnotation() {
            assertThat(ProsperityApplication.class.getAnnotation(SpringBootApplication.class))
                .isNotNull();
        }
    }
    ```

    Format with google-java-format style. Run `./mvnw spotless:apply` after writing.
  </action>
  <acceptance_criteria>
    - ProsperityApplicationTest.java does NOT contain @SpringBootTest
    - ProsperityApplicationTest.java uses reflection to check @SpringBootApplication annotation
    - Test passes with ./mvnw test without a running PostgreSQL
  </acceptance_criteria>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw test -pl . -Dtest="ProsperityApplicationTest" -q 2>&1 | tail -10</automated>
  </verify>
  <done>ProsperityApplicationTest validates @SpringBootApplication annotation via reflection. No Spring context loaded, no PostgreSQL required. Test passes.</done>
</task>

</tasks>

<verification>
1. `cd backend && ./mvnw test -Dtest="ProsperityApplicationTest"` -- passes
2. `cd backend && ./mvnw spotless:check` -- format compliance
</verification>

<success_criteria>
- `./mvnw test` passes ProsperityApplicationTest with 0 failures
- Test does NOT use @SpringBootTest
- Test verifies @SpringBootApplication annotation exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-11-SUMMARY.md`
</output>
