---
phase: 03-accounts-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V009__add_archived_to_bank_accounts.sql
  - backend/src/main/java/com/prosperity/account/Account.java
  - backend/src/main/java/com/prosperity/account/AccessLevel.java
autonomous: true
requirements: [ACCT-05, ACCS-01]
must_haves:
  truths:
    - "Account entity has an 'archived' boolean field defaulting to false"
    - "AccessLevel enum has an isAtLeast() method for hierarchy comparison"
    - "V009 migration adds archived column to bank_accounts table"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V009__add_archived_to_bank_accounts.sql"
      provides: "Flyway migration adding archived column"
      contains: "ALTER TABLE bank_accounts"
    - path: "backend/src/main/java/com/prosperity/account/Account.java"
      provides: "Account entity with archived field"
      contains: "private boolean archived"
    - path: "backend/src/main/java/com/prosperity/account/AccessLevel.java"
      provides: "AccessLevel enum with hierarchy method"
      contains: "isAtLeast"
  key_links: []
---

<objective>
Add the `archived` column to the database schema via Flyway migration, update the Account entity with the archived field, and enhance AccessLevel enum with hierarchy comparison.

Purpose: Establishes the DB and entity foundation that all subsequent plans depend on (archive feature per D-06, access level hierarchy per ACCS-01).
Output: V009 migration file, updated Account.java, updated AccessLevel.java.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-accounts-access-control/03-CONTEXT.md
@.planning/phases/03-accounts-access-control/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V009 migration + Account entity archived field</name>
  <files>
    backend/src/main/resources/db/migration/V009__add_archived_to_bank_accounts.sql,
    backend/src/main/java/com/prosperity/account/Account.java
  </files>
  <read_first>
    backend/src/main/resources/db/migration/V002__create_bank_accounts.sql,
    backend/src/main/resources/db/migration/V008__create_spring_session_tables.sql,
    backend/src/main/java/com/prosperity/account/Account.java
  </read_first>
  <action>
    1. Create `V009__add_archived_to_bank_accounts.sql` with:
       ```sql
       ALTER TABLE bank_accounts
           ADD COLUMN archived BOOLEAN NOT NULL DEFAULT FALSE;
       ```
       Per D-06: column was absent from initial schema, added now.

    2. Update `Account.java` — add archived field after the `updatedAt` field:
       ```java
       @Column(nullable = false)
       private boolean archived = false;
       ```
       Add getter `isArchived()` and setter `setArchived(boolean archived)`.
       Do NOT add archived to the constructor — it defaults to false. The constructor already sets name and accountType.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw compile -q</automated>
  </verify>
  <acceptance_criteria>
    - File `V009__add_archived_to_bank_accounts.sql` exists and contains `ALTER TABLE bank_accounts ADD COLUMN archived BOOLEAN NOT NULL DEFAULT FALSE`
    - `Account.java` contains `private boolean archived = false`
    - `Account.java` contains `public boolean isArchived()`
    - `Account.java` contains `public void setArchived(boolean archived)`
    - `./mvnw compile` succeeds without errors
  </acceptance_criteria>
  <done>V009 migration adds archived column, Account entity has archived field with getter/setter</done>
</task>

<task type="auto">
  <name>Task 2: AccessLevel enum hierarchy method</name>
  <files>backend/src/main/java/com/prosperity/account/AccessLevel.java</files>
  <read_first>
    backend/src/main/java/com/prosperity/account/AccessLevel.java
  </read_first>
  <action>
    Update `AccessLevel.java` to add a hierarchy comparison method. The enum ordinals already encode READ(0) < WRITE(1) < ADMIN(2).

    Add the following method to the enum:
    ```java
    /**
     * Returns true if this access level is at least the required level.
     * Ordering: READ < WRITE < ADMIN.
     */
    public boolean isAtLeast(AccessLevel required) {
        return this.ordinal() >= required.ordinal();
    }
    ```

    The enum declaration order must remain: READ, WRITE, ADMIN (ordinals 0, 1, 2).
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw compile -q</automated>
  </verify>
  <acceptance_criteria>
    - `AccessLevel.java` contains method `public boolean isAtLeast(AccessLevel required)`
    - Method body uses `this.ordinal() >= required.ordinal()`
    - Enum order is READ, WRITE, ADMIN
    - `./mvnw compile` succeeds
  </acceptance_criteria>
  <done>AccessLevel enum supports hierarchy comparison via isAtLeast() method</done>
</task>

</tasks>

<verification>
```bash
cd /home/negus/dev/prosperity/backend && ./mvnw compile -q
```
</verification>

<success_criteria>
- V009 migration file exists with correct ALTER TABLE statement
- Account entity has archived boolean field with default false
- AccessLevel.isAtLeast() method compiles and uses ordinal comparison
</success_criteria>

<output>
After completion, create `.planning/phases/03-accounts-access-control/03-01-SUMMARY.md`
</output>
