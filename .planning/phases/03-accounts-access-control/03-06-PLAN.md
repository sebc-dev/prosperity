---
phase: 03-accounts-access-control
plan: 06
type: execute
wave: 5
depends_on: [03-05]
files_modified:
  - backend/src/test/java/com/prosperity/account/AccountServiceTest.java
  - backend/src/test/java/com/prosperity/account/AccountControllerTest.java
autonomous: true
requirements: [ACCT-01, ACCT-02, ACCT-03, ACCT-04, ACCT-05, ACCS-01, ACCS-02, ACCS-03, ACCS-04]
must_haves:
  truths:
    - "Unit tests verify all AccountService business rules with mocked repositories"
    - "Integration tests verify all AccountController endpoints with real database"
    - "Access control denial scenarios are tested (403 on unauthorized access)"
    - "Last-admin protection is tested"
    - "Archive include/exclude filter is tested"
  artifacts:
    - path: "backend/src/test/java/com/prosperity/account/AccountServiceTest.java"
      provides: "Unit tests for AccountService business logic"
      contains: "class AccountServiceTest"
      min_lines: 100
    - path: "backend/src/test/java/com/prosperity/account/AccountControllerTest.java"
      provides: "Integration tests for AccountController endpoints"
      contains: "class AccountControllerTest"
      min_lines: 100
  key_links:
    - from: "AccountServiceTest.java"
      to: "AccountService.java"
      via: "Mockito @InjectMocks"
      pattern: "@InjectMocks.*AccountService"
    - from: "AccountControllerTest.java"
      to: "AccountController.java"
      via: "MockMvc + Testcontainers"
      pattern: "@SpringBootTest"
---

<objective>
Write unit tests for AccountService and integration tests for AccountController covering all requirements and access control scenarios.

Purpose: Validates all 9 phase requirements (ACCT-01 to ACCT-05, ACCS-01 to ACCS-04) with automated tests. Tests are the acceptance gate for the backend.
Output: AccountServiceTest.java (unit, Mockito) + AccountControllerTest.java (integration, Testcontainers + MockMvc).
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-accounts-access-control/03-CONTEXT.md
@.planning/phases/03-accounts-access-control/03-RESEARCH.md
@.claude/rules/testing-principles.md

<interfaces>
From AccountService.java (after Plan 04):
```java
@Service
public class AccountService {
    AccountResponse createAccount(CreateAccountRequest request, String userEmail);
    List<AccountResponse> getAccounts(boolean includeArchived, String userEmail);
    AccountResponse getAccount(UUID accountId, String userEmail);
    AccountResponse updateAccount(UUID accountId, UpdateAccountRequest request, String userEmail);
    List<AccountAccessResponse> getAccessEntries(UUID accountId, String userEmail);
    AccountAccessResponse setAccess(UUID accountId, SetAccessRequest request, String userEmail);
    void removeAccess(UUID accountId, UUID accessId, String userEmail);
}
```

From AccountController.java (after Plan 05):
```
POST   /api/accounts                          -> 201
GET    /api/accounts(?includeArchived=true)    -> 200
GET    /api/accounts/{id}                     -> 200 | 403 | 404
PATCH  /api/accounts/{id}                     -> 200 | 403 | 404
GET    /api/accounts/{id}/access              -> 200 | 403
POST   /api/accounts/{id}/access              -> 200 | 403
DELETE /api/accounts/{id}/access/{accessId}   -> 204 | 403 | 409
GET    /api/users                             -> 200
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AccountServiceTest unit tests</name>
  <files>backend/src/test/java/com/prosperity/account/AccountServiceTest.java</files>
  <read_first>
    backend/src/test/java/com/prosperity/auth/AuthServiceTest.java,
    backend/src/main/java/com/prosperity/account/AccountService.java,
    backend/src/main/java/com/prosperity/account/AccountRepository.java,
    backend/src/main/java/com/prosperity/account/AccountAccessRepository.java,
    backend/src/main/java/com/prosperity/account/AccessLevel.java,
    backend/src/main/java/com/prosperity/account/Account.java,
    backend/src/main/java/com/prosperity/account/AccountAccess.java,
    backend/src/main/java/com/prosperity/auth/User.java,
    .claude/rules/testing-principles.md
  </read_first>
  <action>
    Create `AccountServiceTest.java` following the same pattern as `AuthServiceTest.java`: `@ExtendWith(MockitoExtension.class)`, `@Mock` repositories, `@InjectMocks AccountService`.

    Use AAA structure (Arrange/Act/Assert) with one blank line between sections. Test naming convention: `scenario_description_and_expected_result`.

    Helper methods for test setup (keep at bottom of class):
    - `createTestUser(String email)` — creates a User with email, "hash", displayName. Use reflection or setId to assign a UUID since constructor doesn't set id.
    - `createTestAccount(String name, AccountType type)` — creates an Account, sets a UUID id via setId.

    Required test cases:

    **Account CRUD (ACCT-01 to ACCT-05):**
    1. `create_account_saves_account_and_returns_response` — verify accountRepository.save called, response has correct fields
    2. `create_account_grants_admin_access_to_creator` — D-04: verify accountAccessRepository.save with AccessLevel.ADMIN
    3. `get_accounts_returns_only_accessible_non_archived` — mock findAllAccessibleByUserId returning Object arrays
    4. `get_accounts_includes_archived_when_requested` — D-07: mock findAllAccessibleByUserIdIncludingArchived
    5. `get_account_throws_access_denied_when_no_access` — D-02: account exists but no access -> AccountAccessDeniedException
    6. `get_account_throws_not_found_when_account_missing` — account doesn't exist -> AccountNotFoundException
    7. `update_account_applies_partial_fields` — D-08: only non-null fields applied
    8. `update_account_rejects_read_only_user` — WRITE required, READ user -> AccountAccessDeniedException

    **Access Management (ACCS-01 to ACCS-04):**
    9. `get_access_entries_requires_admin_level` — non-ADMIN -> AccountAccessDeniedException
    10. `set_access_creates_new_entry_when_not_exists` — new AccountAccess created
    11. `set_access_updates_existing_entry` — existing entry level changed
    12. `remove_access_deletes_entry` — accountAccessRepository.delete called
    13. `remove_access_prevents_removing_last_admin` — countByBankAccountIdAndAccessLevel returns 1, entry is ADMIN -> IllegalStateException

    For mocking `findByIdAndUserId` and `findAllAccessibleByUserId`, return `Object[]` arrays where `[0]` is Account and `[1]` is AccessLevel.

    Mock `userRepository.findByEmail()` to return a test User in all tests that need it.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw test -Dtest=AccountServiceTest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `AccountServiceTest.java` uses `@ExtendWith(MockitoExtension.class)` (no Spring context)
    - At least 13 test methods covering all 9 requirements
    - Test `create_account_grants_admin_access_to_creator` verifies ArgumentCaptor captures AccessLevel.ADMIN
    - Test `get_account_throws_access_denied_when_no_access` verifies AccountAccessDeniedException is thrown
    - Test `remove_access_prevents_removing_last_admin` verifies IllegalStateException is thrown
    - All tests follow AAA structure with single-line Act
    - All tests pass: `./mvnw test -Dtest=AccountServiceTest` exits 0
  </acceptance_criteria>
  <done>13+ unit tests covering all AccountService business rules pass</done>
</task>

<task type="auto">
  <name>Task 2: AccountControllerTest integration tests</name>
  <files>backend/src/test/java/com/prosperity/account/AccountControllerTest.java</files>
  <read_first>
    backend/src/test/java/com/prosperity/auth/AuthControllerTest.java,
    backend/src/test/java/com/prosperity/TestcontainersConfig.java,
    backend/src/main/java/com/prosperity/account/AccountController.java,
    backend/src/main/java/com/prosperity/account/AccountService.java,
    backend/src/main/java/com/prosperity/auth/SecurityConfig.java,
    .claude/rules/testing-principles.md
  </read_first>
  <action>
    Create `AccountControllerTest.java` following the same pattern as `AuthControllerTest.java`: `@SpringBootTest`, `@AutoConfigureMockMvc`, `@ActiveProfiles("test")`, `@Import(TestcontainersConfig.class)`, `@DirtiesContext(classMode = ClassMode.AFTER_EACH_TEST_METHOD)`.

    Use `MockMvc` with `.with(user("admin@test.com"))` and `.with(csrf())` for authenticated requests.

    Each test needs to set up its own data. Create a helper method `setupAdminWithAccount()` that:
    1. Creates a User via `UserRepository.save()`
    2. Creates an Account via `AccountRepository.save()`
    3. Creates AccountAccess with ADMIN via `AccountAccessRepository.save()`

    Inject: `MockMvc`, `UserRepository`, `AccountRepository`, `AccountAccessRepository`.

    Required test cases:

    **Account CRUD:**
    1. `create_personal_account_returns_201_with_admin_access` — POST /api/accounts with PERSONAL type
    2. `create_shared_account_returns_201_and_creator_has_admin` — POST with SHARED
    3. `list_accounts_returns_only_accessible_accounts` — create 2 accounts, only grant access to 1, verify only 1 returned
    4. `list_accounts_excludes_archived_by_default` — create archived account, GET /api/accounts returns empty
    5. `list_accounts_includes_archived_when_requested` — GET /api/accounts?includeArchived=true returns archived account
    6. `get_account_returns_403_when_no_access` — D-02: GET /api/accounts/{id} for account without access -> 403
    7. `update_account_changes_name` — PATCH /api/accounts/{id} with new name
    8. `update_account_archives_account` — D-08: PATCH with archived=true
    9. `update_account_returns_403_for_read_only_user` — user with READ access tries PATCH -> 403

    **Access Management:**
    10. `list_access_returns_entries_for_admin` — GET /api/accounts/{id}/access
    11. `list_access_returns_403_for_non_admin` — user with WRITE (not ADMIN) tries listing access -> 403
    12. `set_access_grants_access_to_new_user` — POST /api/accounts/{id}/access
    13. `remove_access_returns_204` — DELETE /api/accounts/{id}/access/{accessId}
    14. `remove_last_admin_returns_409` — delete the only ADMIN entry -> 409

    **Users endpoint:**
    15. `list_users_returns_all_users` — GET /api/users

    Use `MediaType.APPLICATION_JSON` for all POST/PATCH requests. Verify response JSON with `jsonPath()`.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity/backend && ./mvnw test -Dtest=AccountControllerTest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `AccountControllerTest.java` uses `@SpringBootTest`, `@AutoConfigureMockMvc`, `@Import(TestcontainersConfig.class)`
    - At least 15 test methods covering CRUD, access management, and users endpoint
    - Test `get_account_returns_403_when_no_access` verifies `.andExpect(status().isForbidden())`
    - Test `remove_last_admin_returns_409` verifies `.andExpect(status().isConflict())`
    - Test `list_accounts_returns_only_accessible_accounts` creates 2 accounts, grants access to 1, verifies response has exactly 1 account
    - All tests pass: `./mvnw test -Dtest=AccountControllerTest` exits 0
  </acceptance_criteria>
  <done>15+ integration tests covering all REST endpoints and access control scenarios pass</done>
</task>

</tasks>

<verification>
```bash
cd /home/negus/dev/prosperity/backend && ./mvnw test -Dtest="AccountServiceTest,AccountControllerTest" -q 2>&1 | tail -10
```
</verification>

<success_criteria>
- AccountServiceTest: 13+ unit tests, all green
- AccountControllerTest: 15+ integration tests, all green
- Full backend test suite still passes: `./mvnw test`
- Access control denial (403), not-found (404), and conflict (409) scenarios covered
</success_criteria>

<output>
After completion, create `.planning/phases/03-accounts-access-control/03-06-SUMMARY.md`
</output>
