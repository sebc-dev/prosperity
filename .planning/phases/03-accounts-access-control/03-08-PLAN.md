---
phase: 03-accounts-access-control
plan: 08
type: execute
wave: 7
depends_on: [03-07]
files_modified:
  - frontend/src/app/accounts/accounts.ts
  - frontend/src/app/accounts/accounts.spec.ts
  - frontend/src/app/accounts/account-dialog.ts
  - frontend/src/app/accounts/account-dialog.spec.ts
autonomous: true
requirements: [ACCT-01, ACCT-02, ACCT-03, ACCT-04, ACCT-05]
must_haves:
  truths:
    - "Account list page shows p-table with Nom, Type, Solde, Statut, Actions columns"
    - "Toggle switch shows/hides archived accounts"
    - "Ajouter un compte button opens create dialog"
    - "Create/edit dialog saves account via AccountService"
    - "Archive action triggers confirmation dialog"
  artifacts:
    - path: "frontend/src/app/accounts/accounts.ts"
      provides: "Account list page with p-table and archive toggle"
      contains: "class Accounts"
      min_lines: 50
    - path: "frontend/src/app/accounts/account-dialog.ts"
      provides: "Create/edit account dialog with p-dialog"
      contains: "class AccountDialog"
      min_lines: 30
  key_links:
    - from: "accounts.ts"
      to: "account.service.ts"
      via: "inject(AccountService)"
      pattern: "accountService.loadAccounts"
    - from: "account-dialog.ts"
      to: "account.service.ts"
      via: "inject(AccountService)"
      pattern: "accountService.createAccount\\|accountService.updateAccount"
---

<objective>
Build the accounts list page component with p-table, archive toggle, and the create/edit account dialog.

Purpose: Implements the main UI for account management (ACCT-01 to ACCT-05). Users can see their accounts, create new ones, edit existing ones, and archive them.
Output: accounts.ts (list page), account-dialog.ts (create/edit), with tests.
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-accounts-access-control/03-CONTEXT.md
@.planning/phases/03-accounts-access-control/03-UI-SPEC.md

<interfaces>
From account.service.ts (Plan 07):
```typescript
@Injectable({ providedIn: 'root' })
export class AccountService {
    readonly accounts: Signal<AccountResponse[]>;
    loadAccounts(includeArchived?: boolean): Observable<AccountResponse[]>;
    createAccount(request: CreateAccountRequest): Observable<AccountResponse>;
    updateAccount(id: string, request: UpdateAccountRequest): Observable<AccountResponse>;
}
```

From account.types.ts (Plan 07):
```typescript
export type AccountType = 'PERSONAL' | 'SHARED';
export type AccessLevel = 'READ' | 'WRITE' | 'ADMIN';
export interface AccountResponse { id, name, accountType, balance, currency, archived, createdAt, currentUserAccessLevel }
export interface CreateAccountRequest { name, accountType }
export interface UpdateAccountRequest { name?, accountType?, archived? }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accounts list page component</name>
  <files>
    frontend/src/app/accounts/accounts.ts,
    frontend/src/app/accounts/accounts.spec.ts
  </files>
  <read_first>
    frontend/src/app/accounts/accounts.ts,
    frontend/src/app/accounts/account.service.ts,
    frontend/src/app/accounts/account.types.ts,
    frontend/src/app/auth/login.ts,
    frontend/src/app/auth/setup.ts,
    frontend/src/app/dashboard/dashboard.ts,
    .planning/phases/03-accounts-access-control/03-UI-SPEC.md
  </read_first>
  <action>
    Replace the placeholder `accounts.ts` with the full implementation per UI-SPEC.

    Component structure:
    - `@Component` with `ChangeDetectionStrategy.OnPush`, standalone
    - Imports: `TableModule` (primeng/table), `ButtonModule` (primeng/button), `TagModule` (primeng/tag), `ToggleSwitchModule` (primeng/toggleswitch), `ConfirmDialogModule` (primeng/confirmdialog), `MessageModule` (primeng/message), `TooltipModule` (primeng/tooltip), `FormsModule`, `CommonModule`, `AccountDialog`
    - Inject: `AccountService`, `ConfirmationService` (primeng/api)
    - Provide: `ConfirmationService` in the component's `providers` array

    Signals and state:
    ```typescript
    private readonly accountService = inject(AccountService);
    private readonly confirmationService = inject(ConfirmationService);

    protected includeArchived = signal(false);
    protected loading = signal(true);
    protected error = signal<string | null>(null);

    protected accounts = this.accountService.accounts;

    // Dialog state
    protected dialogVisible = signal(false);
    protected editingAccount = signal<AccountResponse | null>(null);
    ```

    Lifecycle — use constructor or `effect` to load accounts:
    ```typescript
    constructor() {
        this.loadData();
    }
    ```

    Methods:
    - `loadData()` — calls `accountService.loadAccounts(this.includeArchived())`, sets loading/error signals
    - `onToggleArchived()` — called when toggle changes, calls loadData()
    - `openCreateDialog()` — sets `editingAccount(null)`, `dialogVisible(true)`
    - `openEditDialog(account: AccountResponse)` — sets `editingAccount(account)`, `dialogVisible(true)`
    - `onDialogSaved()` — calls `loadData()` to refresh list
    - `confirmArchive(account: AccountResponse)` — uses `confirmationService.confirm()` with UI-SPEC copy: accept label "Archiver", message per spec, then calls `accountService.updateAccount(account.id, { archived: true })`
    - `unarchive(account: AccountResponse)` — calls `accountService.updateAccount(account.id, { archived: false })` directly, then `loadData()`

    Template (per UI-SPEC layout):
    ```html
    <div class="p-8">
      <!-- Header row -->
      <div class="flex items-center justify-between mb-6">
        <h1 class="text-2xl font-semibold">Comptes bancaires</h1>
        <p-button
          label="Ajouter un compte"
          icon="pi pi-plus"
          (onClick)="openCreateDialog()"
        />
      </div>

      <!-- Archive toggle -->
      <div class="flex items-center justify-end gap-2 mb-4">
        <label for="archiveToggle" class="text-sm">Afficher les archives</label>
        <p-toggleswitch
          [(ngModel)]="includeArchived"
          (onChange)="onToggleArchived()"
          inputId="archiveToggle"
          aria-label="Afficher les comptes archives"
        />
      </div>

      <!-- Error message -->
      @if (error()) {
        <p-message severity="error" [text]="error()!" styleClass="mb-4 w-full" />
      }

      <!-- Empty state -->
      @if (!loading() && accounts().length === 0) {
        <div role="status" class="text-center py-12">
          <h2 class="text-lg font-semibold mb-2">Aucun compte</h2>
          <p class="text-muted-color mb-4">Creez votre premier compte bancaire pour commencer a suivre vos finances.</p>
          <p-button label="Ajouter un compte" icon="pi pi-plus" (onClick)="openCreateDialog()" />
        </div>
      }

      <!-- Table -->
      @if (accounts().length > 0) {
        <p-table
          [value]="accounts()"
          [stripedRows]="true"
          [sortField]="'name'"
          [sortOrder]="1"
          styleClass="p-datatable-sm"
        >
          <ng-template pTemplate="header">
            <tr>
              <th pSortableColumn="name">Nom <p-sortIcon field="name" /></th>
              <th>Type</th>
              <th pSortableColumn="balance">Solde <p-sortIcon field="balance" /></th>
              <th>Statut</th>
              <th>Actions</th>
            </tr>
          </ng-template>
          <ng-template pTemplate="body" let-account>
            <tr [class.text-muted-color]="account.archived">
              <td>{{ account.name }}</td>
              <td>
                <p-tag
                  [value]="account.accountType === 'SHARED' ? 'Commun' : 'Personnel'"
                  [severity]="account.accountType === 'SHARED' ? 'info' : 'secondary'"
                />
              </td>
              <td class="font-tabular-nums">
                {{ account.balance | number:'1.2-2':'fr-FR' }} {{ account.currency }}
              </td>
              <td>
                <p-tag
                  [value]="account.archived ? 'Archive' : 'Actif'"
                  [severity]="account.archived ? 'warn' : 'success'"
                />
              </td>
              <td>
                <div class="flex gap-1">
                  @if (account.currentUserAccessLevel === 'WRITE' || account.currentUserAccessLevel === 'ADMIN') {
                    <p-button
                      icon="pi pi-pencil"
                      [text]="true"
                      severity="secondary"
                      [pTooltip]="'Modifier ' + account.name"
                      (onClick)="openEditDialog(account)"
                      [attr.aria-label]="'Modifier ' + account.name"
                    />
                  }
                  @if (account.currentUserAccessLevel === 'ADMIN' && account.accountType === 'SHARED') {
                    <p-button
                      icon="pi pi-users"
                      [text]="true"
                      severity="secondary"
                      [pTooltip]="'Gerer les acces de ' + account.name"
                      (onClick)="openAccessDialog(account)"
                      [attr.aria-label]="'Gerer les acces de ' + account.name"
                    />
                  }
                  @if (account.currentUserAccessLevel === 'ADMIN') {
                    @if (account.archived) {
                      <p-button
                        icon="pi pi-replay"
                        [text]="true"
                        severity="secondary"
                        pTooltip="Desarchiver"
                        (onClick)="unarchive(account)"
                        [attr.aria-label]="'Desarchiver ' + account.name"
                      />
                    } @else {
                      <p-button
                        icon="pi pi-inbox"
                        [text]="true"
                        severity="danger"
                        [pTooltip]="'Archiver ' + account.name"
                        (onClick)="confirmArchive(account)"
                        [attr.aria-label]="'Archiver ' + account.name"
                      />
                    }
                  }
                </div>
              </td>
            </tr>
          </ng-template>
        </p-table>
      }

      <!-- Confirm dialog -->
      <p-confirmdialog />

      <!-- Create/Edit dialog -->
      <app-account-dialog
        [visible]="dialogVisible()"
        [account]="editingAccount()"
        (visibleChange)="dialogVisible.set($event)"
        (saved)="onDialogSaved()"
      />
    </div>
    ```

    Note: `openAccessDialog(account)` will be implemented in Plan 09. For now, add the method signature with a TODO comment:
    ```typescript
    protected accessDialogVisible = signal(false);
    protected accessDialogAccount = signal<AccountResponse | null>(null);

    openAccessDialog(account: AccountResponse): void {
        this.accessDialogAccount.set(account);
        this.accessDialogVisible.set(true);
    }
    ```

    For `accounts.spec.ts`: create basic tests verifying the component creates, shows heading, and calls loadAccounts on init. Use `provideHttpClientTesting` and verify no real HTTP calls. Minimum 3 tests:
    1. `should create the component`
    2. `should display page heading Comptes bancaires`
    3. `should call loadAccounts on initialization`

    Note about p-toggleswitch: it uses `[(ngModel)]` so you need `FormsModule` in imports. The signal `includeArchived` should be a simple boolean field (not a signal) if used with `ngModel`, OR use a regular boolean and update separately. Since `p-toggleswitch` uses two-way binding with ngModel, use a plain property:
    ```typescript
    protected includeArchived = false;
    ```
    Then in `onToggleArchived()`:
    ```typescript
    onToggleArchived(): void {
        this.loadData();
    }
    ```
    And loadAccounts call uses `this.includeArchived` directly.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity/frontend && npx ng build --configuration=development 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `accounts.ts` has `ChangeDetectionStrategy.OnPush` and standalone component
    - Template contains `p-table` with columns Nom, Type, Solde, Statut, Actions
    - Template contains `p-toggleswitch` with label "Afficher les archives"
    - Template contains "Ajouter un compte" button
    - Template contains empty state with "Aucun compte" heading
    - Action buttons use `pi-pencil`, `pi-users`, `pi-inbox` icons per UI-SPEC
    - `p-tag` shows "Commun"/"Personnel" for type and "Actif"/"Archive" for status
    - Archive confirmation uses `ConfirmationService`
    - `accounts.spec.ts` has at least 3 passing tests
    - Frontend compiles without errors
  </acceptance_criteria>
  <done>Accounts list page shows p-table with all UI-SPEC columns, badges, action buttons, archive toggle, and empty state</done>
</task>

<task type="auto">
  <name>Task 2: Create/Edit account dialog component</name>
  <files>
    frontend/src/app/accounts/account-dialog.ts,
    frontend/src/app/accounts/account-dialog.spec.ts
  </files>
  <read_first>
    frontend/src/app/accounts/accounts.ts,
    frontend/src/app/accounts/account.service.ts,
    frontend/src/app/accounts/account.types.ts,
    frontend/src/app/auth/setup.ts,
    .planning/phases/03-accounts-access-control/03-UI-SPEC.md
  </read_first>
  <action>
    Create `account-dialog.ts` — a p-dialog component for creating and editing accounts per UI-SPEC.

    Component inputs/outputs:
    ```typescript
    @Component({
        selector: 'app-account-dialog',
        changeDetection: ChangeDetectionStrategy.OnPush,
        standalone: true,
        imports: [DialogModule, FloatLabelModule, InputTextModule, SelectModule, ButtonModule, MessageModule, ReactiveFormsModule],
        template: `...`,
    })
    export class AccountDialog {
        visible = input(false);
        account = input<AccountResponse | null>(null);

        visibleChange = output<boolean>();
        saved = output<void>();
    }
    ```

    Use Angular `input()` and `output()` signal-based APIs.

    Form setup with reactive forms:
    ```typescript
    private readonly accountService = inject(AccountService);
    private readonly fb = inject(FormBuilder);

    protected form = this.fb.group({
        name: ['', [Validators.required, Validators.maxLength(100)]],
        accountType: ['PERSONAL' as AccountType, Validators.required],
    });

    protected loading = signal(false);
    protected error = signal<string | null>(null);

    protected isEdit = computed(() => this.account() !== null);
    protected dialogHeader = computed(() => this.isEdit() ? 'Modifier le compte' : 'Ajouter un compte');
    ```

    Type options for p-select:
    ```typescript
    protected typeOptions = [
        { label: 'Personnel', value: 'PERSONAL' },
        { label: 'Commun', value: 'SHARED' },
    ];
    ```

    Watch for account input changes using `effect`:
    ```typescript
    constructor() {
        effect(() => {
            const acct = this.account();
            if (acct) {
                this.form.patchValue({ name: acct.name, accountType: acct.accountType });
            } else {
                this.form.reset({ name: '', accountType: 'PERSONAL' });
            }
            this.error.set(null);
        });
    }
    ```

    Save method:
    ```typescript
    protected onSave(): void {
        if (this.form.invalid) return;
        this.loading.set(true);
        this.error.set(null);

        const { name, accountType } = this.form.getRawValue();
        const acct = this.account();

        const request$ = acct
            ? this.accountService.updateAccount(acct.id, { name: name!, accountType: accountType! })
            : this.accountService.createAccount({ name: name!, accountType: accountType! });

        request$.subscribe({
            next: () => {
                this.loading.set(false);
                this.visibleChange.emit(false);
                this.saved.emit();
            },
            error: () => {
                this.loading.set(false);
                this.error.set("Impossible d'enregistrer les modifications. Veuillez reessayer.");
            },
        });
    }

    protected onHide(): void {
        this.visibleChange.emit(false);
    }
    ```

    Template (per UI-SPEC create/edit dialog layout):
    ```html
    <p-dialog
      [header]="dialogHeader()"
      [visible]="visible()"
      (onHide)="onHide()"
      [modal]="true"
      [closable]="true"
      [draggable]="false"
      styleClass="max-w-lg w-full"
    >
      <form [formGroup]="form" (ngSubmit)="onSave()" class="flex flex-col gap-4 p-6">
        <p-floatlabel variant="on">
          <input pInputText id="accountName" formControlName="name" class="w-full" />
          <label for="accountName">Nom du compte</label>
        </p-floatlabel>
        @if (form.controls.name.touched && form.controls.name.errors) {
          @if (form.controls.name.errors['required']) {
            <small class="text-red-500">Le nom du compte est requis</small>
          }
          @if (form.controls.name.errors['maxlength']) {
            <small class="text-red-500">Le nom ne peut pas depasser 100 caracteres</small>
          }
        }

        <p-floatlabel variant="on">
          <p-select
            id="accountType"
            formControlName="accountType"
            [options]="typeOptions"
            optionLabel="label"
            optionValue="value"
            styleClass="w-full"
          />
          <label for="accountType">Type de compte</label>
        </p-floatlabel>

        @if (error()) {
          <p-message severity="error" [text]="error()!" />
        }
      </form>

      <ng-template pTemplate="footer">
        <div class="flex justify-end gap-2">
          <p-button
            label="Annuler"
            [text]="true"
            severity="secondary"
            (onClick)="onHide()"
          />
          <p-button
            [label]="loading() ? 'Enregistrement...' : 'Enregistrer le compte'"
            [loading]="loading()"
            [disabled]="form.invalid || loading()"
            (onClick)="onSave()"
          />
        </div>
      </ng-template>
    </p-dialog>
    ```

    For `account-dialog.spec.ts`: basic tests:
    1. `should create the component`
    2. `should show Ajouter un compte header when no account input`
    3. `should disable save button when form is invalid`
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity/frontend && pnpm test --run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `account-dialog.ts` has `ChangeDetectionStrategy.OnPush`, standalone
    - Uses `input()` and `output()` signal APIs
    - Uses `ReactiveFormsModule` with `Validators.required` and `Validators.maxLength(100)` on name
    - Dialog header changes between "Ajouter un compte" and "Modifier le compte" based on `isEdit` computed
    - Save button text changes to "Enregistrement..." when loading
    - Validation messages match UI-SPEC: "Le nom du compte est requis", "Le nom ne peut pas depasser 100 caracteres"
    - Cancel button is secondary text style, save button is primary
    - `account-dialog.spec.ts` has at least 3 passing tests
    - All frontend tests pass: `pnpm test --run` exits 0
  </acceptance_criteria>
  <done>Create/edit dialog with form validation, loading states, and error handling per UI-SPEC</done>
</task>

</tasks>

<verification>
```bash
cd /home/negus/dev/prosperity/frontend && npx ng build --configuration=development 2>&1 | tail -5 && pnpm test --run 2>&1 | tail -10
```
</verification>

<success_criteria>
- Accounts page shows p-table with all specified columns and action buttons
- Create dialog opens, validates, and saves accounts
- Edit dialog pre-fills form and updates account
- Archive confirmation dialog works
- Archive toggle shows/hides archived accounts
- All frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-accounts-access-control/03-08-SUMMARY.md`
</output>
