---
phase: 05-transactions
plan: 04
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - backend/src/main/java/com/prosperity/transaction/TransactionService.java
  - backend/src/test/java/com/prosperity/transaction/TransactionControllerTest.java
  - backend/src/test/java/com/prosperity/recurring/RecurringTemplateControllerTest.java
autonomous: true
requirements: [TXNS-01, TXNS-02, TXNS-03, TXNS-04, TXNS-05, TXNS-06, TXNS-07, TXNS-08]
must_haves:
  truths:
    - "Split transaction amounts sum to parent transaction amount"
    - "Transaction with splits has category_id set to null"
    - "Integration tests prove CRUD, pagination, filtering, access control, splits, recurring, and pointage all work end-to-end"
  artifacts:
    - path: "backend/src/test/java/com/prosperity/transaction/TransactionControllerTest.java"
      provides: "Integration tests for transaction CRUD + pagination + filters + pointage + splits"
      contains: "class TransactionControllerTest"
    - path: "backend/src/test/java/com/prosperity/recurring/RecurringTemplateControllerTest.java"
      provides: "Integration tests for recurring template CRUD + generate"
      contains: "class RecurringTemplateControllerTest"
  key_links:
    - from: "TransactionControllerTest.java"
      to: "TransactionController endpoints"
      via: "MockMvc HTTP calls"
      pattern: "mockMvc\\.perform"
    - from: "TransactionService.java"
      to: "TransactionSplitRepository"
      via: "split management in setSplits method"
      pattern: "transactionSplitRepository"
---

<objective>
Add split transaction logic to TransactionService and write comprehensive integration tests for all transaction and recurring template endpoints.

Purpose: Splits are the most complex business rule in Phase 5 (sum validation, parent category nullification per D-06). Integration tests validate the entire stack from HTTP to database for all 8 TXNS requirements.
Output: Split logic in TransactionService + 2 integration test files covering all requirements
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-transactions/05-CONTEXT.md
@.planning/phases/05-transactions/05-RESEARCH.md
@.planning/phases/05-transactions/05-01-SUMMARY.md
@.planning/phases/05-transactions/05-02-SUMMARY.md
@.planning/phases/05-transactions/05-03-SUMMARY.md

<interfaces>
<!-- Split DTOs from Plan 01 -->
TransactionSplitRequest(@NotNull UUID categoryId, @NotNull BigDecimal amount, @Size(max=500) String description)
TransactionSplitResponse(UUID id, UUID categoryId, String categoryName, BigDecimal amount, String description)

<!-- Existing test patterns -->
From backend/src/test/java/com/prosperity/category/CategoryControllerTest.java:
- Uses @SpringBootTest + @AutoConfigureMockMvc + Testcontainers
- @Import(TestcontainersConfig.class)
- MockMvc with perform().andExpect()
- Setup creates users via userRepository, authenticates via SecurityMockMvcRequestPostProcessors
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add split transaction logic to TransactionService + split endpoints to TransactionController</name>
  <files>
    backend/src/main/java/com/prosperity/transaction/TransactionService.java,
    backend/src/main/java/com/prosperity/transaction/TransactionController.java
  </files>
  <read_first>
    backend/src/main/java/com/prosperity/transaction/TransactionService.java,
    backend/src/main/java/com/prosperity/transaction/TransactionController.java,
    backend/src/main/java/com/prosperity/transaction/TransactionSplit.java,
    backend/src/main/java/com/prosperity/transaction/TransactionSplitRepository.java,
    backend/src/main/java/com/prosperity/transaction/TransactionSplitRequest.java,
    backend/src/main/java/com/prosperity/transaction/TransactionSplitResponse.java,
    backend/src/main/java/com/prosperity/category/CategoryRepository.java,
    backend/src/main/java/com/prosperity/shared/Money.java
  </read_first>
  <action>
    **TransactionService.java — add split management methods:**

    1. Add `setSplits(UUID transactionId, List<TransactionSplitRequest> splits, String userEmail)` method returning `TransactionResponse`:
       - @Transactional
       - resolveUser, find transaction by id
       - requireAccountAccess(transaction.getBankAccount().getId(), userId, WRITE)
       - Validate splits sum equals transaction amount (per D-05, Pitfall 4 — use compareTo not equals):
         ```java
         BigDecimal splitSum = splits.stream()
             .map(TransactionSplitRequest::amount)
             .reduce(BigDecimal.ZERO, BigDecimal::add);
         if (splitSum.compareTo(transaction.getAmount().amount()) != 0) {
             throw new IllegalArgumentException(
                 "La somme des splits (" + splitSum + ") ne correspond pas au montant de la transaction (" + transaction.getAmount().amount() + ")");
         }
         ```
       - Delete existing splits: `transactionSplitRepository.deleteByTransactionId(transactionId)`
       - Flush after delete to avoid constraint violations: `transactionSplitRepository.flush()`
       - For each split request: resolve category (throw CategoryNotFoundException if missing), create TransactionSplit(transaction, category, new Money(split.amount())), set description, save
       - Set transaction.category to null (per D-06: splits replace single category)
       - Save transaction
       - Return toResponse(transaction)

    2. Add `clearSplits(UUID transactionId, String userEmail)` method returning `TransactionResponse`:
       - @Transactional
       - resolveUser, find transaction by id
       - requireAccountAccess(transaction.getBankAccount().getId(), userId, WRITE)
       - `transactionSplitRepository.deleteByTransactionId(transactionId)`
       - Return toResponse(transaction)

    3. Add `getSplits(UUID transactionId, String userEmail)` method returning `List<TransactionSplitResponse>`:
       - @Transactional(readOnly = true)
       - resolveUser, find transaction by id
       - requireAccountAccess(transaction.getBankAccount().getId(), userId, READ)
       - Return transactionSplitRepository.findByTransactionId(transactionId).stream().map(this::toSplitResponse).toList()

    **TransactionController.java — add split endpoints:**

    4. Add PUT `/api/transactions/{id}/splits`:
       ```java
       @PutMapping("/transactions/{id}/splits")
       public TransactionResponse setSplits(
           @PathVariable UUID id,
           @Valid @RequestBody List<TransactionSplitRequest> splits,
           Principal principal) {
           return transactionService.setSplits(id, splits, principal.getName());
       }
       ```

    5. Add DELETE `/api/transactions/{id}/splits`:
       ```java
       @DeleteMapping("/transactions/{id}/splits")
       public TransactionResponse clearSplits(@PathVariable UUID id, Principal principal) {
           return transactionService.clearSplits(id, principal.getName());
       }
       ```

    6. Add GET `/api/transactions/{id}/splits`:
       ```java
       @GetMapping("/transactions/{id}/splits")
       public List<TransactionSplitResponse> getSplits(@PathVariable UUID id, Principal principal) {
           return transactionService.getSplits(id, principal.getName());
       }
       ```

    7. Add IllegalArgumentException handler to TransactionController:
       ```java
       @ExceptionHandler(IllegalArgumentException.class)
       public ResponseEntity<Map<String, String>> handleIllegalArgument(IllegalArgumentException e) {
           return ResponseEntity.status(HttpStatus.BAD_REQUEST).body(Map.of("error", e.getMessage()));
       }
       ```
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity && ./mvnw compile -pl backend -q</automated>
  </verify>
  <acceptance_criteria>
    - TransactionService.java contains "public TransactionResponse setSplits(UUID transactionId, List<TransactionSplitRequest> splits, String userEmail)"
    - TransactionService.java contains "splitSum.compareTo(transaction.getAmount().amount()) != 0"
    - TransactionService.java contains "transaction.setCategory(null)" in setSplits (per D-06)
    - TransactionService.java contains "public TransactionResponse clearSplits("
    - TransactionService.java contains "public List<TransactionSplitResponse> getSplits("
    - TransactionController.java contains "@PutMapping(\"/transactions/{id}/splits\")"
    - TransactionController.java contains "@DeleteMapping(\"/transactions/{id}/splits\")"
    - TransactionController.java contains "@GetMapping(\"/transactions/{id}/splits\")"
    - TransactionController.java contains "IllegalArgumentException" handler
    - `./mvnw compile -pl backend -q` exits 0
  </acceptance_criteria>
  <done>Split logic validates sum equals parent amount, nullifies parent category, and has REST endpoints for set/clear/get splits</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for TransactionController and RecurringTemplateController</name>
  <files>
    backend/src/test/java/com/prosperity/transaction/TransactionControllerTest.java,
    backend/src/test/java/com/prosperity/recurring/RecurringTemplateControllerTest.java
  </files>
  <read_first>
    backend/src/test/java/com/prosperity/category/CategoryControllerTest.java,
    backend/src/test/java/com/prosperity/account/AccountControllerTest.java,
    backend/src/test/java/com/prosperity/TestcontainersConfig.java,
    backend/src/main/java/com/prosperity/transaction/TransactionController.java,
    backend/src/main/java/com/prosperity/transaction/TransactionService.java,
    backend/src/main/java/com/prosperity/recurring/RecurringTemplateController.java,
    backend/src/main/java/com/prosperity/recurring/RecurringTemplateService.java,
    backend/src/main/java/com/prosperity/transaction/CreateTransactionRequest.java,
    backend/src/main/java/com/prosperity/transaction/TransactionResponse.java,
    backend/src/main/java/com/prosperity/transaction/TransactionFilterParams.java,
    backend/src/main/java/com/prosperity/recurring/CreateRecurringTemplateRequest.java
  </read_first>
  <behavior>
    TransactionControllerTest:
    - create_manual_transaction_returns_201_with_response (TXNS-01)
    - create_transaction_without_write_access_returns_403 (access control)
    - create_transaction_on_nonexistent_account_returns_404 (access control)
    - create_transaction_with_invalid_category_returns_404 (validation)
    - get_transactions_paginated_returns_page_with_content (TXNS-08)
    - get_transactions_filtered_by_date_range (TXNS-07)
    - get_transactions_filtered_by_amount_range (TXNS-07)
    - get_transactions_filtered_by_category (TXNS-07)
    - get_transactions_filtered_by_search_text (TXNS-07)
    - update_manual_transaction_returns_updated_response (TXNS-02)
    - update_non_manual_transaction_returns_400 (TXNS-02 guard)
    - delete_manual_transaction_returns_204 (TXNS-03)
    - delete_non_manual_transaction_returns_400 (TXNS-03 guard)
    - toggle_pointed_flips_boolean (TXNS-05)
    - set_splits_with_valid_sum_returns_transaction_with_null_category (TXNS-06)
    - set_splits_with_invalid_sum_returns_400 (TXNS-06 validation)
    - clear_splits_returns_transaction (TXNS-06)

    RecurringTemplateControllerTest:
    - create_template_returns_201 (TXNS-04)
    - list_active_templates_returns_only_active (TXNS-04)
    - update_template_returns_updated (TXNS-04)
    - delete_template_returns_204 (TXNS-04)
    - generate_transaction_creates_recurring_source_and_advances_date (TXNS-04)
    - generate_from_inactive_template_returns_400 (TXNS-04)
    - create_template_without_access_returns_403 (access control)
  </behavior>
  <action>
    **TransactionControllerTest.java:**
    - Annotation: `@SpringBootTest`, `@AutoConfigureMockMvc`, `@Import(TestcontainersConfig.class)`, `@Transactional` (rollback after each test)
    - Inject: `MockMvc`, `UserRepository`, `AccountRepository`, `AccountAccessRepository`, `TransactionRepository`, `CategoryRepository`, `ObjectMapper`
    - `@BeforeEach` setup: Create a test user via `new User("test@test.com", "Test", passwordEncoder.encode("pass"), "ADMIN")`, save it. Create a test Account, save it. Create AccountAccess with WRITE level for the test user. Create a test Category (root, non-system).
    - Use `SecurityMockMvcRequestPostProcessors.user("test@test.com")` for authenticated requests.
    - For access control tests: create a second user with NO access to the account.

    Test details:
    - `create_manual_transaction_returns_201_with_response`: POST `/api/accounts/{accountId}/transactions` with JSON `{"amount": -45.30, "transactionDate": "2026-04-07", "description": "Courses Lidl", "categoryId": "{catId}"}`. Expect 201, response contains amount, description, source="MANUAL", state="MANUAL_UNMATCHED", pointed=false.
    - `get_transactions_paginated_returns_page_with_content`: Create 25 transactions directly via repo, GET with page=0&size=10. Expect 200 with `content.length == 10`, `totalElements == 25`, `totalPages == 3`.
    - `get_transactions_filtered_by_date_range`: Create transactions on 3 different dates, filter with dateFrom/dateTo. Expect only matching transactions returned.
    - `get_transactions_filtered_by_search_text`: Create transactions with different descriptions, search="Lidl". Expect only matching transaction.
    - `toggle_pointed_flips_boolean`: Create transaction (pointed=false), PATCH `/api/transactions/{id}/pointed`, expect pointed=true. PATCH again, expect pointed=false.
    - `set_splits_with_valid_sum_returns_transaction_with_null_category`: Create transaction with amount=-100.00 and a category. PUT `/api/transactions/{id}/splits` with 2 splits summing to -100.00. Expect response has categoryId=null and splits array with 2 entries.
    - `set_splits_with_invalid_sum_returns_400`: PUT splits that sum to -90.00 on a -100.00 transaction. Expect 400.
    - `delete_non_manual_transaction_returns_400`: Create transaction with source=RECURRING directly via repo. DELETE. Expect 400 with error message.

    **RecurringTemplateControllerTest.java:**
    - Same annotations and setup pattern as TransactionControllerTest.
    - `create_template_returns_201`: POST to `/api/accounts/{accountId}/recurring-templates` with JSON `{"amount": -800.00, "description": "Loyer", "frequency": "MONTHLY", "dayOfMonth": 5, "nextDueDate": "2026-05-05"}`. Expect 201 with all fields.
    - `generate_transaction_creates_recurring_source_and_advances_date`: Create template with nextDueDate=2026-05-05 and frequency=MONTHLY. POST `.../generate`. Expect 201, response has source="RECURRING", transactionDate="2026-05-05". GET template: nextDueDate should be "2026-06-05".
    - `generate_from_inactive_template_returns_400`: Create template, update active=false. POST generate. Expect 400.

    Follow testing-principles.md: AAA structure, single Act line, descriptive snake_case names, no mocks (real DB via Testcontainers), boundary values for split sum validation.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity && ./mvnw test -pl backend -Dtest="TransactionControllerTest,RecurringTemplateControllerTest" -q</automated>
  </verify>
  <acceptance_criteria>
    - TransactionControllerTest.java contains "class TransactionControllerTest"
    - TransactionControllerTest.java contains "@SpringBootTest" and "@AutoConfigureMockMvc"
    - TransactionControllerTest.java contains "@Import(TestcontainersConfig.class)"
    - TransactionControllerTest.java contains "create_manual_transaction_returns_201"
    - TransactionControllerTest.java contains "get_transactions_paginated_returns_page_with_content"
    - TransactionControllerTest.java contains "toggle_pointed_flips_boolean"
    - TransactionControllerTest.java contains "set_splits_with_valid_sum"
    - TransactionControllerTest.java contains "set_splits_with_invalid_sum_returns_400"
    - TransactionControllerTest.java contains "create_transaction_without_write_access_returns_403"
    - RecurringTemplateControllerTest.java contains "class RecurringTemplateControllerTest"
    - RecurringTemplateControllerTest.java contains "generate_transaction_creates_recurring_source_and_advances_date"
    - RecurringTemplateControllerTest.java contains "generate_from_inactive_template_returns_400"
    - `./mvnw test -pl backend -Dtest="TransactionControllerTest,RecurringTemplateControllerTest" -q` exits 0
  </acceptance_criteria>
  <done>All integration tests pass: transaction CRUD, pagination, 6 filter types, access control 403/404, split sum validation, pointage toggle, recurring template CRUD + generate</done>
</task>

</tasks>

<verification>
- `./mvnw test -pl backend -Dtest="TransactionControllerTest,RecurringTemplateControllerTest" -q` exits 0
- All 8 TXNS requirements have at least one test covering them
- Access control tested for both 403 (no access) and 404 (account does not exist) cases
- Split sum validation tested with both valid and invalid sums
</verification>

<success_criteria>
- Split logic: sum validation with compareTo, parent category nullified, flush between delete and insert
- 17+ transaction controller tests covering TXNS-01/02/03/05/06/07/08
- 7+ recurring template tests covering TXNS-04
- All tests use Testcontainers PostgreSQL (not H2)
- No mocks on repositories — full integration through real database
</success_criteria>

<output>
After completion, create `.planning/phases/05-transactions/05-04-SUMMARY.md`
</output>
