---
phase: 05-transactions
plan: 06
type: execute
wave: 4
depends_on: [05-04, 05-05]
files_modified: []
autonomous: false
requirements: [TXNS-01, TXNS-02, TXNS-03, TXNS-05, TXNS-07, TXNS-08]
must_haves:
  truths:
    - "Full backend test suite passes with all 8 TXNS requirements covered"
    - "Frontend builds and transaction UI is visually correct"
    - "End-to-end flow works: create, edit, delete, filter, paginate, toggle pointed"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete transaction feature. All backend tests pass, frontend builds, and the user can perform all transaction operations through the UI.

Purpose: Ensure the full vertical slice works end-to-end before closing Phase 5.
Output: Human-verified approval of the transaction management interface
</objective>

<execution_context>
@/home/negus/dev/prosperity/.claude/get-shit-done/workflows/execute-plan.md
@/home/negus/dev/prosperity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-transactions/05-UI-SPEC.md
@.planning/phases/05-transactions/05-04-SUMMARY.md
@.planning/phases/05-transactions/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any remaining issues</name>
  <files></files>
  <read_first>
    backend/src/test/java/com/prosperity/transaction/TransactionControllerTest.java,
    backend/src/test/java/com/prosperity/recurring/RecurringTemplateControllerTest.java
  </read_first>
  <action>
    Run the full backend test suite: `./mvnw verify -q`
    Run the full frontend test suite: `cd frontend && pnpm test -- --run`
    Run the frontend build: `cd frontend && pnpm build`

    If any tests fail, investigate and fix the root cause. Common issues:
    - MoneyConverter not applied to JPQL filter parameters — switch to BigDecimal
    - Missing SecurityConfig endpoint permissions for new /api/accounts/{accountId}/transactions paths
    - PrimeNG import name mismatches (DatePicker vs Calendar in v21)
    - Sidebar AccountService import path
    - Route parameter extraction issues

    Also verify that the existing Phase 4 test (CategoryControllerTest) still passes — regression check.

    Check SecurityConfig to ensure new endpoints are permitted for authenticated users:
    - `/api/accounts/*/transactions/**` needs to be accessible
    - `/api/transactions/**` needs to be accessible
    - `/api/accounts/*/recurring-templates/**` needs to be accessible
    If SecurityConfig uses `.anyRequest().authenticated()` pattern, no changes needed. If it uses explicit matchers, add the new paths.
  </action>
  <verify>
    <automated>cd /home/negus/dev/prosperity && ./mvnw verify -q && cd frontend && pnpm test -- --run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw verify -q` exits 0 (all backend tests pass including Checkstyle, JaCoCo)
    - `pnpm test -- --run` exits 0 (all frontend tests pass)
    - `pnpm build` exits 0 (production build succeeds)
    - No regression on existing Phase 3/4 tests
  </acceptance_criteria>
  <done>Full test suite green, frontend production build succeeds, no regressions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of transaction UI</name>
  <files></files>
  <action>
    Present the transaction management interface for human verification. The user will test the complete flow through the browser.
  </action>
  <verify>User confirms approval</verify>
  <done>User types "approved" after verifying the transaction UI matches the UI-SPEC contract</done>
  <what-built>
    Complete transaction management interface:
    - Backend: Transaction CRUD with access control, paginated filtered listing, pointage toggle, split transactions, recurring templates with generate
    - Frontend: Transaction list page with p-table lazy pagination, filter bar (6 filters), create/edit dialog, amount coloring (red/green), pointed toggle, per-account sidebar navigation
    - Tests: Integration tests for all endpoints + component tests for frontend
  </what-built>
  <how-to-verify>
    1. Start the dev environment: `docker compose up -d` (DB), `./mvnw spring-boot:run` (backend), `cd frontend && pnpm dev` (frontend)
    2. Log in with your existing user
    3. Navigate to an account's transactions via sidebar link
    4. Verify empty state: "Aucune transaction" message with CTA button
    5. Click "Ajouter une transaction" — fill in: Amount=-45.30, Date=today, Description="Courses Lidl", Category=Alimentation
    6. Verify transaction appears in table with: red amount, category name, date formatted dd/MM/yyyy, source badge "Manuel"
    7. Add 2-3 more transactions with different amounts and dates
    8. Test filters: search "Lidl", date range, amount range, category filter
    9. Click pencil icon to edit a transaction — change description, verify update
    10. Click trash icon — confirm deletion, verify transaction removed
    11. Click the Pointed column on a transaction — verify checkmark appears/disappears
    12. Verify pagination works with > 20 transactions (create enough or change page size to 10)
    13. Verify non-manual transactions (if any RECURRING exist from template generate) do NOT show edit/delete buttons
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All backend and frontend tests pass
- Frontend production build succeeds
- User visually confirms the transaction interface matches UI-SPEC
</verification>

<success_criteria>
- Full test suite green
- Transaction CRUD works end-to-end through the UI
- Filters and pagination work correctly
- Pointed toggle works
- Amount coloring matches UI-SPEC (red negative, green positive)
- Copy matches UI-SPEC copywriting contract
</success_criteria>

<output>
After completion, create `.planning/phases/05-transactions/05-06-SUMMARY.md`
</output>
