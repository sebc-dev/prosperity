/** Nom du produit affiché dans le chrome et le <title>. */
export const APP_NAME = 'Prosperity'

/** Tagline affichée dans le footer. */
export const APP_TAGLINE = 'gestion de budget familial'

/** Chemin vers le logo mark (gland Chêne) — servi depuis /public. */
export const APP_LOGO_MARK = '/logo-mark.svg'
