# E08.5 — Réconciliation dépense confirmable ↔ consommation budget

> **Durée estimée** : 1-2 jours
> **Statut** : not started
> **Dépend de** : E08
> **Bloque** : E11 (le livrable overflow F10 exige qu'une transaction confirmée consomme un budget)
> **ADRs activés** : [0017](../adr/0017-confirmable-expense-vs-budget-consumption.md) (nouveau)
> **Suivi** : [#133](https://github.com/sebc-dev/prosperity/issues/133)

---

## Objectif

Lever la contradiction de modélisation documentée par l'ADR 0017 : la règle de confirmation E07 (`assert_expenses_categorized` refuse **toute** jambe `category_id IS NULL` d'une dépense non-transfert) et le modèle de consommation E08 (`compute_consumption` présuppose une **forme canonique à jambe compte `category_id = NULL`** + jambe catégorie) sont mutuellement exclusifs — **aucune transaction confirmable par le service ne peut faire monter une consommation budget**. Prouvé par `test_confirm_planned_categorised_200` (nette à 0) et `test_confirm_uncategorised_expense_422` (rejetée) ; toute la suite budget masque le mur en seedant la forme consommatrice en DB directe.

Option 1 de l'ADR retenue : **marqueur structurel de rôle de jambe sur `Split`**. La jambe `funding` (mouvement de compte) peut être non catégorisée ; seules les jambes `classification` exigent une catégorie. La forme canonique consommatrice devient alors confirmable via le flux réel.

Livrable agrégé : une dépense saisie via HTTP (`POST /accounts/{id}/transactions` → `POST /transactions/{id}/confirm`) en forme canonique (jambe `funding` compte + jambe `classification` catégorie) se confirme **et** fait monter la consommation du budget de la catégorie — sans seed DB.

---

## Stories

### S08.5.1 — Marqueur `leg_role` sur `Split` + migration

**Livrable observable** : colonne `splits.leg_role` peuplée, sync rules à jour.

| Phase | Description | Diff |
|---|---|---|
| **P08.5.1.1** | `Split` (`models.py` + Pydantic `domain.py`) : `leg_role` Literal['funding','classification'] non-NULL. Migration `0013_split_leg_role.py` (`down_revision = "0012"`) en deux temps (colonne nullable + back-fill déterministe `category_id IS NULL → 'funding'`, sinon `'classification'`, puis `NOT NULL`). Pas de sync rules PowerSync à éditer (aucun artefact n'existe encore — client PowerSync = E14, cf. ADR 0003) : la colonne est server-authoritative et sync-safe, incluse quand les sync rules seront écrites. Test niveau 1 schema check + test back-fill | ~120 |

---

### S08.5.2 — Règle de confirm réécrite (TDD)

**Livrable observable** : la forme canonique consommatrice (jambe `funding` NULL + jambe `classification` catégorisée) passe `transition_to_confirmed` ; une jambe `classification` NULL est toujours refusée.

| Phase | Description | Diff |
|---|---|---|
| **P08.5.2.1** | `domain.py` : `assert_expenses_categorized` n'exige une catégorie que sur les jambes `leg_role == 'classification'` (la jambe `funding` peut être NULL). Glossaire CONTEXT.md §`splits.category_id NULL` mis à jour (« tout split *de classification* doit avoir une catégorie »). Tests example + Hypothesis (toute jambe classification NULL → refus ; funding NULL → accepté) | ~120 |
| **P08.5.2.2** | Réécriture des tests de transition (`test_confirm_planned_categorised_200` → forme canonique B confirmable ; `test_confirm_uncategorised_expense_422` → cible une jambe classification NULL). `tests/factories/sqlalchemy.py` : `SplitFactory`/`TransactionFactory` produisent la forme canonique B par défaut (funding + classification) | ~120 |

---

### S08.5.3 — Repilotage des seeds budget via le flux confirm réel

**Livrable observable** : les scénarios E08 deviennent pilotables par le flux confirm HTTP/service réel, plus par seed DB.

| Phase | Description | Diff |
|---|---|---|
| **P08.5.3.1** | Bascule des seeds budget (`_add_confirmed_expense`, scénarios threshold S08.3.4 de `test_budget_threshold_alerts.py`) vers le flux confirm réel (`transition_to_confirmed`) au lieu de l'écriture DB directe. Suppression du contournement `_add_planned_trigger` (net 0) devenu inutile | ~150 |
| **P08.5.3.2** | Parcours E2E backend `tests/e2e/test_budget_lifecycle.py` (chaîne de valeur E06+E07+E08 en HTTP boîte-noire via `committed_client` : confirmer tx → consommation monte → seuil franchi → drill-down → RBAC non-fuite shared). Helpers manquants dans `tests/e2e/_helpers.py` (`create_account`, `create_transaction` + `confirm`, `create_budget`). Cible finale = parcours Playwright en E15 (stratégie de tests §6.2) | ~200 |

---

## Récapitulatif

| ID | Type | Diff | Cumul |
|---|---|---|---|
| S08.5.1 (1 phase) | `leg_role` + migration + sync rules | 120 | 120 |
| S08.5.2 (2 phases) | Règle confirm réécrite + factories | 240 | 360 |
| S08.5.3 (2 phases) | Repilotage seeds + parcours E2E | 350 | 710 |
| **Total** | **3 stories / 5 phases** | **~710 lignes** | |

---

## Critères d'acceptation

- [ ] Une dépense en forme canonique (jambe `funding` compte NULL + jambe `classification` catégorisée) se confirme via le service **et** fait monter la consommation du budget de sa catégorie
- [ ] Une jambe `classification` sans catégorie est toujours refusée à `confirmed` (`UncategorizedExpenseError`)
- [ ] Les scénarios threshold S08.3.4 sont pilotés via le flux confirm réel (plus de seed DB / plus de `_add_planned_trigger` net 0)
- [ ] Le parcours E2E `test_budget_lifecycle.py` passe (consommation produite par le flux HTTP, pas par seed)

---

## Notes pour l'implémenteur

- 🔒 **Changement de modèle pris au coût minimal.** La migration `0013` + le changement de sync rules + le back-fill arrivent **avant qu'aucune instance ne soit en prod** (roadmap `not started`). C'est l'argument central de l'option 1 vs l'option 2 (relâchement par convention sans colonne) dans l'ADR 0017.
- ⚠️ **E08 (budgets) n'a rien à changer** : son modèle de consommation (`_consumption_filters`, exclusion de la jambe `category_id IS NULL`) présuppose déjà la forme canonique B. C'est E07 qui doit la produire de façon confirmable — cet epic clôt l'écart en faveur du code budget déjà écrit.
- ⚠️ **`is_transfer` inchangé** : un transfert (≥ 2 comptes) reste exempté de la catégorisation indépendamment de `leg_role`. La nouvelle règle ne touche que les non-transferts.
- La forme canonique d'une dépense devient **B** (funding + classification) et non plus la forme « 2 jambes même catégorie » qu'imposaient les seuls tests confirmables — les factories convergent vers cette forme unique.
- 🔒 **`leg_role` assigné côté serveur, jamais depuis le payload client.** Sinon un client pourrait marquer une vraie jambe de dépense comme `funding` pour échapper à `assert_expenses_categorized` (contournement de la catégorisation obligatoire). Le service (`add_split` / route create) dérive le rôle de la structure canonique. Invariant à pinner : une transaction non-transfert a **au plus une** jambe `funding`, qui équilibre les jambes `classification` (le zero-sum reste enforced par ailleurs).
- ⚠️ **`budget` ⊥ `transactions` (contrat import-linter #1, même layer).** La réconciliation est **interne au module `transactions`** : `compute_consumption` (côté budget) filtre déjà `category_id ∈ subtree`, ce qui exclut nativement la jambe `funding` (catégorie NULL) — **aucun changement budget, aucun nouvel import cross-module**.
