import { ChangeDetectionStrategy, Component } from '@angular/core';

/**
 * Envelope details page stub.
 *
 * Implemented in Plan 08 (frontend-pages). This stub keeps the lazy route
 * typecheck-clean between Plan 07 (infrastructure) and Plan 08 (pages).
 */
@Component({
  selector: 'app-envelope-details-page',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `<p>Envelope details page (stub — implemented in Plan 08).</p>`,
})
export class EnvelopeDetailsPage {}
